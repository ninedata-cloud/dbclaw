# PostgreSQL 内置知识库文档 - 20篇专业运维诊断文档
# 供 AI 诊断引擎调用，每篇明确标注相关 skill

POSTGRESQL_DOCS = [
    {
        "category": "综合诊断",
        "title": "PostgreSQL 数据库综合诊断流程",
        "content": "# PostgreSQL 数据库综合诊断流程\n\n## 概述\n\n综合诊断是对 PostgreSQL 数据库健康状态进行全面评估的系统化流程，适用于数据库出现未知性能问题、告警触发或周期性巡检场景。从宏观到微观，结合各专项 skill 的调用顺序与判断标准。\n\n## 第一步：快速健康检查\n\n### 调用 `get_db_status` skill\n\n```sql\nSELECT version();\nSELECT pg_postmaster_start_time(), now() - pg_postmaster_start_time() AS uptime;\nSELECT count(*) AS current_connections,\n       (SELECT setting::int FROM pg_settings WHERE name = 'max_connections') AS max_conn,\n       round(count(*) * 100.0 / (SELECT setting::int FROM pg_settings WHERE name = 'max_connections'), 1) AS pct\nFROM pg_stat_activity;\nSELECT state, count(*) FROM pg_stat_activity GROUP BY state ORDER BY count DESC;\n```\n\n| 指标 | 正常范围 | 告警阈值 | 说明 |\n|------|----------|----------|------|\n| 连接使用率 | < 70% | > 85% | 接近上限需检查连接池配置 |\n| idle in transaction | < 5 | > 20 | 事务未提交，可能持有锁 |\n| 活跃查询数 | < CPU核数x2 | > 核数x4 | 并发过高 |\n\n## 第二步：操作系统指标检查\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n- CPU > 80% 持续 5 分钟 -> 进入 CPU 高负载诊断流程\n- io_wait > 30% -> 进入写入慢诊断流程\n- 可用内存 < 总内存 10% 且 Swap 在使用 -> 内存压力，检查 shared_buffers 与 work_mem\n- 磁盘使用率 > 80% -> 进入空间占用高诊断流程\n\n## 第三步：当前会话与锁分析\n\n### 调用 `get_process_list` skill\n\n```sql\nSELECT pid, usename, application_name, client_addr,\n       state, wait_event_type, wait_event,\n       now() - query_start AS duration,\n       left(query, 150) AS query_snippet\nFROM pg_stat_activity\nWHERE state != 'idle' AND pid != pg_backend_pid()\nORDER BY duration DESC NULLS LAST LIMIT 30;\n```\n\n重点识别：wait_event_type = 'Lock' 为锁等待；state = 'idle in transaction' 持续时间长表示事务未提交。\n\n### 调用 `execute_diagnostic_query` skill 检查锁阻塞链\n\n```sql\nSELECT blocked.pid AS blocked_pid, blocked.usename,\n       blocking.pid AS blocking_pid, blocking.usename AS blocking_user,\n       now() - blocked.query_start AS blocked_duration,\n       blocked.query AS blocked_query\nFROM pg_stat_activity blocked\nJOIN pg_stat_activity blocking ON blocking.pid = ANY(pg_blocking_pids(blocked.pid))\nWHERE cardinality(pg_blocking_pids(blocked.pid)) > 0;\n```\n\n## 第四步：慢查询分析\n\n### 调用 `get_slow_queries` skill\n\n```sql\nSELECT left(query, 100) AS query, calls,\n       round(total_exec_time::numeric, 2) AS total_ms,\n       round(mean_exec_time::numeric, 2) AS mean_ms,\n       shared_blks_hit, shared_blks_read, blk_read_time\nFROM pg_stat_statements\nORDER BY total_exec_time DESC LIMIT 20;\n```\n\n## 第五步：空间使用检查\n\n### 调用 `get_db_size` skill\n\n```sql\nSELECT datname, pg_size_pretty(pg_database_size(datname)) AS size\nFROM pg_database ORDER BY pg_database_size(datname) DESC;\n\nSELECT schemaname, tablename,\n       pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS total\nFROM pg_tables\nWHERE schemaname NOT IN ('pg_catalog','information_schema')\nORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC LIMIT 20;\n```\n\n## 第六步：表膨胀与 autovacuum 状态\n\n### 调用 `get_table_stats` skill\n\n```sql\nSELECT schemaname, relname, n_live_tup, n_dead_tup,\n       round(n_dead_tup * 100.0 / NULLIF(n_live_tup + n_dead_tup, 0), 2) AS dead_pct,\n       last_autovacuum, last_autoanalyze\nFROM pg_stat_user_tables\nORDER BY n_dead_tup DESC LIMIT 20;\n```\n\ndead_pct > 20% 需立即 VACUUM；last_autovacuum IS NULL 且表很大说明 autovacuum 未运行。\n\n## 第七步：复制状态检查\n\n### 调用 `get_replication_status` skill\n\n```sql\nSELECT client_addr, application_name, state,\n       pg_size_pretty(sent_lsn - replay_lsn) AS lag_size,\n       write_lag, flush_lag, replay_lag\nFROM pg_stat_replication;\n```\n\nreplay_lag > 60s -> 复制延迟告警；备库缺失 -> 检查 pg_hba.conf 与网络连通性。\n\n## 第八步：关键配置核查\n\n### 调用 `get_db_variables` skill\n\n```sql\nSELECT name, setting, unit, short_desc FROM pg_settings\nWHERE name IN (\n  'max_connections','shared_buffers','work_mem','maintenance_work_mem',\n  'effective_cache_size','checkpoint_completion_target','wal_level',\n  'max_wal_senders','autovacuum','log_min_duration_statement'\n);\n```\n\n| 参数 | 推荐值 | 说明 |\n|------|--------|------|\n| shared_buffers | 物理内存 25% | 数据库缓冲区 |\n| effective_cache_size | 物理内存 75% | 优化器参考值 |\n| work_mem | 16~64MB | 排序/哈希内存，注意连接数乘数效应 |\n| checkpoint_completion_target | 0.9 | 平滑检查点写入 |\n\n## 诊断结论模板\n\n1. 健康评分（0-100）\n2. 发现的问题（Critical / Warning / Info）\n3. 根因分析\n4. 优化建议（含具体 SQL 或配置修改）\n5. 跟踪计划",
    },
    {
        "category": "性能诊断",
        "title": "PostgreSQL CPU使用高诊断优化流程",
        "content": "# PostgreSQL CPU使用高诊断优化流程\n\n## 概述\n\nPostgreSQL CPU 使用率高通常由全表扫描、缺少索引、复杂排序/聚合、大量并发短查询、autovacuum 密集运行等原因引起。\n\n## 第一步：确认 CPU 使用来源\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n```bash\nps aux --sort=-%cpu | grep postgres | head -20\ntop -bn1 | grep postgres\n```\n\n判断依据：\n- 多个 postgres 进程 CPU 均高 -> 并发查询密集型\n- 单个进程接近 100% -> 单条重型查询\n- autovacuum worker 进程 CPU 高 -> 表膨胀触发密集清理\n\n## 第二步：定位高 CPU 查询\n\n### 调用 `get_process_list` skill\n\n```sql\nSELECT pid, usename, now() - query_start AS duration,\n       state, wait_event_type, left(query, 200) AS query\nFROM pg_stat_activity\nWHERE state = 'active' AND pid != pg_backend_pid()\nORDER BY duration DESC NULLS LAST;\n```\n\n### 调用 `get_slow_queries` skill（需启用 pg_stat_statements）\n\n```sql\nSELECT left(query, 120) AS query, calls,\n       round(total_exec_time::numeric, 2) AS total_ms,\n       round(mean_exec_time::numeric, 2) AS mean_ms,\n       rows / NULLIF(calls, 0) AS avg_rows,\n       shared_blks_hit, shared_blks_read\nFROM pg_stat_statements\nWHERE mean_exec_time > 500\nORDER BY total_exec_time DESC LIMIT 20;\n```\n\n## 第三步：执行计划分析\n\n### 调用 `explain_query` skill\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS, VERBOSE, FORMAT TEXT)\nSELECT * FROM orders WHERE customer_id = 12345 AND status = 'pending';\n```\n\n| 节点类型 | 问题标志 | 优化方向 |\n|----------|----------|----------|\n| Seq Scan | 大表全扫 | 添加索引 |\n| Hash Join | hash batches > 1 | 增大 work_mem |\n| Sort | 磁盘排序 | 增大 work_mem 或添加覆盖索引 |\n| Nested Loop | rows 估算严重偏差 | 更新统计信息 ANALYZE |\n\n```sql\n-- 检查统计信息是否陈旧\nSELECT schemaname, tablename, n_live_tup, last_analyze, last_autoanalyze\nFROM pg_stat_user_tables\nWHERE last_analyze < now() - interval '7 days' OR last_analyze IS NULL\nORDER BY n_live_tup DESC LIMIT 20;\n```\n\n## 第四步：autovacuum 检查\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 正在运行的 autovacuum 进程\nSELECT pid, datname, relid::regclass AS table_name,\n       phase, heap_blks_scanned, heap_blks_vacuumed\nFROM pg_stat_progress_vacuum;\n\n-- 死元组最多的表\nSELECT relname, n_dead_tup, n_live_tup,\n       round(n_dead_tup * 100.0 / NULLIF(n_live_tup + n_dead_tup, 0), 2) AS dead_pct,\n       last_autovacuum\nFROM pg_stat_user_tables\nWHERE n_dead_tup > 10000\nORDER BY n_dead_tup DESC LIMIT 15;\n```\n\n## 第五步：索引使用率分析\n\n### 调用 `get_table_stats` skill\n\n```sql\n-- 未使用的索引（浪费维护开销，增加写入 CPU）\nSELECT schemaname, relname, indexrelname, idx_scan,\n       pg_size_pretty(pg_relation_size(indexrelid)) AS index_size\nFROM pg_stat_user_indexes\nWHERE idx_scan = 0 AND schemaname NOT IN ('pg_catalog','information_schema')\nORDER BY pg_relation_size(indexrelid) DESC;\n\n-- 顺序扫描次数高于索引扫描的大表\nSELECT relname, seq_scan, idx_scan, n_live_tup, seq_tup_read\nFROM pg_stat_user_tables\nWHERE seq_scan > idx_scan AND n_live_tup > 50000\nORDER BY seq_tup_read DESC LIMIT 20;\n```\n\n## 优化方案\n\n### 短期应急\n\n```sql\nSELECT pg_cancel_backend(pid) FROM pg_stat_activity\nWHERE state = 'active' AND now() - query_start > interval '5 minutes';\nSELECT pg_terminate_backend(pid) FROM pg_stat_activity\nWHERE state = 'active' AND now() - query_start > interval '10 minutes';\n```\n\n### 中期优化\n\n```sql\nCREATE INDEX CONCURRENTLY idx_orders_cust_status ON orders(customer_id, status);\nCREATE INDEX CONCURRENTLY idx_orders_pending ON orders(created_at) WHERE status = 'pending';\nANALYZE VERBOSE orders;\nSET work_mem = '64MB';\n```\n\n### 长期优化\n\n1. 配置 pg_stat_statements 持续监控 TOP SQL\n2. 调整 autovacuum：autovacuum_vacuum_scale_factor = 0.05\n3. 读写分离，分析查询路由到只读副本\n4. 对热点表启用表分区减少全表扫描范围\n5. 增大 shared_buffers 和 effective_cache_size 提高缓存命中率",
    },
    {
        "category": "性能诊断",
        "title": "PostgreSQL 空间占用高诊断优化流程",
        "content": "# PostgreSQL 空间占用高诊断优化流程\n\n## 概述\n\nPostgreSQL 空间持续增长的常见原因：表膨胀（dead tuples）、索引膨胀、WAL 文件堆积、临时文件过大、日志文件未轮转、大对象（Large Object）未清理。\n\n## 第一步：整体空间使用分析\n\n### 调用 `get_db_size` skill\n\n```sql\n-- 各数据库大小\nSELECT datname, pg_size_pretty(pg_database_size(datname)) AS size\nFROM pg_database ORDER BY pg_database_size(datname) DESC;\n\n-- TOP 30 最大表（含索引）\nSELECT schemaname, tablename,\n       pg_size_pretty(pg_relation_size(schemaname||'.'||tablename)) AS table_size,\n       pg_size_pretty(pg_indexes_size(schemaname||'.'||tablename)) AS index_size,\n       pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS total_size\nFROM pg_tables\nWHERE schemaname NOT IN ('pg_catalog','information_schema')\nORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC LIMIT 30;\n```\n\n## 第二步：检查操作系统磁盘使用\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n重点检查：\n- PostgreSQL data 目录使用率（$PGDATA/base）\n- WAL 目录（$PGDATA/pg_wal）占用\n- 临时文件目录（pgsql_tmp）\n- 日志目录（$PGDATA/log 或 /var/log/postgresql）\n\n```bash\ndu -sh $PGDATA/pg_wal\ndu -sh $PGDATA/base\ndu -sh $PGDATA/pg_stat_tmp\ndf -h\n```\n\n## 第三步：表膨胀诊断\n\n### 调用 `get_table_stats` skill\n\n```sql\n-- 查看各表膨胀率\nSELECT relname, n_live_tup, n_dead_tup,\n       round(n_dead_tup * 100.0 / NULLIF(n_live_tup + n_dead_tup, 0), 2) AS dead_pct,\n       pg_size_pretty(pg_total_relation_size(relid)) AS total_size,\n       last_autovacuum, last_vacuum\nFROM pg_stat_user_tables\nWHERE n_live_tup + n_dead_tup > 0\nORDER BY n_dead_tup DESC LIMIT 20;\n```\n\n**处理方案**：\n```sql\n-- 手动 VACUUM（不阻塞读写）\nVACUUM (VERBOSE, ANALYZE) table_name;\n\n-- 强制回收空间（需要 AccessExclusiveLock，会短暂锁表）\nVACUUM FULL table_name;\n\n-- 在线重建大表（推荐，使用 pg_repack 扩展）\n-- pg_repack -t table_name dbname\n```\n\n## 第四步：索引膨胀检查\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 检查索引膨胀（需要 pgstattuple 扩展）\nSELECT indexrelname,\n       pg_size_pretty(pg_relation_size(indexrelid)) AS index_size,\n       idx_scan, idx_tup_read\nFROM pg_stat_user_indexes\nORDER BY pg_relation_size(indexrelid) DESC LIMIT 20;\n\n-- 重建索引（在线，不锁表）\n-- REINDEX INDEX CONCURRENTLY idx_name;\n```\n\n## 第五步：WAL 文件堆积检查\n\n### 调用 `get_db_variables` skill\n\n```sql\nSELECT name, setting, unit FROM pg_settings\nWHERE name IN (\n  'wal_level','archive_mode','archive_command',\n  'max_wal_size','min_wal_size','wal_keep_size',\n  'max_slot_wal_keep_size'\n);\n\n-- 检查复制槽是否阻止 WAL 清理\nSELECT slot_name, plugin, slot_type, active,\n       pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) AS lag\nFROM pg_replication_slots;\n```\n\n**复制槽堆积处理**：\n```sql\n-- 删除不再使用的复制槽（谨慎操作）\nSELECT pg_drop_replication_slot('slot_name');\n```\n\n## 第六步：临时文件检查\n\n### 调用 `get_db_status` skill\n\n```sql\nSELECT datname, temp_files, temp_bytes,\n       pg_size_pretty(temp_bytes) AS temp_size\nFROM pg_stat_database\nWHERE temp_bytes > 0\nORDER BY temp_bytes DESC;\n```\n\ntemp_bytes 持续增长 -> work_mem 不足，排序/哈希溢出磁盘。\n\n## 优化建议\n\n| 问题 | 处理方案 |\n|------|----------|\n| 表膨胀 | VACUUM FULL 或 pg_repack |\n| 索引膨胀 | REINDEX CONCURRENTLY |\n| WAL 堆积 | 检查复制槽，调整 max_wal_size |\n| 临时文件多 | 增大 work_mem |\n| 日志过大 | 调整 log_rotation_age/size |\n\n```sql\n-- 调整 work_mem 减少临时文件\nALTER SYSTEM SET work_mem = '32MB';\nSELECT pg_reload_conf();\n\n-- 配置日志轮转\n-- postgresql.conf:\n-- log_rotation_age = 1d\n-- log_rotation_size = 100MB\n```",
    },
    {
        "category": "性能诊断",
        "title": "PostgreSQL 网络流量高诊断优化流程",
        "content": "# PostgreSQL 网络流量高诊断优化流程\n\n## 概述\n\nPostgreSQL 网络流量高通常由大结果集查询、频繁小查询、批量数据导出、复制流量或连接风暴引起。需结合 OS 网络指标与数据库内部统计定位根因。\n\n## 第一步：确认网络使用情况\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n```bash\n# 查看网络带宽使用\nnetstat -i\nifstat -i eth0 1 5\n# 或使用 sar\nsar -n DEV 1 5\n\n# 查看连接数\nss -s\nnetstat -tn | grep 5432 | wc -l\n```\n\n关注：\n- 网络 TX/RX 超过网卡带宽 70% -> 流量过高\n- 大量 ESTABLISHED 连接 -> 连接风暴或连接泄漏\n- TIME_WAIT 过多 -> 客户端频繁建立短连接\n\n## 第二步：定位高流量查询\n\n### 调用 `get_slow_queries` skill\n\n```sql\n-- 返回行数最多的查询（高流量来源）\nSELECT left(query, 120) AS query, calls,\n       rows, rows / NULLIF(calls, 0) AS avg_rows,\n       round(total_exec_time::numeric, 2) AS total_ms,\n       shared_blks_hit + shared_blks_read AS total_blks\nFROM pg_stat_statements\nORDER BY rows DESC LIMIT 20;\n\n-- 平均返回行数最多的查询\nSELECT left(query, 120) AS query, calls,\n       rows / NULLIF(calls, 0) AS avg_rows_per_call\nFROM pg_stat_statements\nWHERE calls > 10\nORDER BY avg_rows_per_call DESC LIMIT 20;\n```\n\n### 调用 `get_process_list` skill\n\n```sql\n-- 查看当前正在传输大量数据的连接\nSELECT pid, usename, application_name, client_addr,\n       state, wait_event,\n       now() - query_start AS duration,\n       left(query, 150) AS query\nFROM pg_stat_activity\nWHERE state IN ('active', 'idle in transaction')\nORDER BY duration DESC NULLS LAST LIMIT 20;\n```\n\n## 第三步：检查复制流量\n\n### 调用 `get_replication_status` skill\n\n```sql\nSELECT client_addr, application_name, state,\n       sent_lsn, write_lsn, flush_lsn, replay_lsn,\n       pg_size_pretty(sent_lsn - replay_lsn) AS lag_bytes,\n       write_lag, flush_lag, replay_lag\nFROM pg_stat_replication;\n\n-- 逻辑复制槽\nSELECT slot_name, plugin, active, confirmed_flush_lsn,\n       pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), confirmed_flush_lsn)) AS pending\nFROM pg_replication_slots;\n```\n\n复制延迟大 -> WAL 积压，主库持续向备库发送大量数据。\n\n## 第四步：连接数分析\n\n### 调用 `get_db_status` skill\n\n```sql\nSELECT count(*) AS total,\n       sum(CASE WHEN state = 'active' THEN 1 ELSE 0 END) AS active,\n       sum(CASE WHEN state = 'idle' THEN 1 ELSE 0 END) AS idle,\n       sum(CASE WHEN state = 'idle in transaction' THEN 1 ELSE 0 END) AS idle_in_txn,\n       (SELECT setting::int FROM pg_settings WHERE name = 'max_connections') AS max_conn\nFROM pg_stat_activity;\n\n-- 按应用名统计连接\nSELECT application_name, client_addr, count(*) AS conn_count\nFROM pg_stat_activity\nGROUP BY application_name, client_addr\nORDER BY conn_count DESC LIMIT 20;\n```\n\n## 第五步：优化方案\n\n### 减少查询返回数据量\n\n```sql\n-- 添加 LIMIT，避免全表返回\nSELECT * FROM large_table WHERE condition LIMIT 1000;\n\n-- 只查询需要的列\nSELECT id, name, status FROM user WHERE active = true;\n\n-- 使用分页\nSELECT * FROM orders ORDER BY created_at DESC LIMIT 100 OFFSET 0;\n```\n\n### 连接池优化\n\n部署 PgBouncer 连接池，减少连接数与连接建立开销：\n\n```ini\n; pgbouncer.ini\n[databases]\nmydb = host=127.0.0.1 port=5432 dbname=mydb\n\n[pgbouncer]\npool_mode = transaction\nmax_client_conn = 1000\ndefault_pool_size = 20\n```\n\n### 压缩网络传输\n\n```sql\n-- 在 postgresql.conf 中开启 SSL 压缩（有一定 CPU 开销）\n-- ssl_compression = on  (PostgreSQL 14+ 已废弃)\n\n-- 应用层：使用流式游标减少内存和网络占用\nDECLARE my_cursor CURSOR FOR SELECT * FROM large_table;\nFETCH 1000 FROM my_cursor;\n```\n\n### 复制流量优化\n\n```sql\n-- 检查并限制复制槽 WAL 保留量\nALTER SYSTEM SET max_slot_wal_keep_size = '10GB';\nSELECT pg_reload_conf();\n```",
    },
    {
        "category": "性能诊断",
        "title": "PostgreSQL SQL诊断优化流程",
        "content": "# PostgreSQL SQL诊断优化流程\n\n## 概述\n\nSQL 性能问题是 PostgreSQL 最常见的问题类型，包括：执行计划不优（全表扫描、错误 Join 策略）、统计信息陈旧、参数不合理、索引缺失或失效。\n\n## 第一步：发现问题 SQL\n\n### 调用 `get_slow_queries` skill\n\n```sql\n-- TOP SQL by 总执行时间\nSELECT left(query, 150) AS query, calls,\n       round(total_exec_time::numeric, 2) AS total_ms,\n       round(mean_exec_time::numeric, 2) AS mean_ms,\n       round(stddev_exec_time::numeric, 2) AS stddev_ms,\n       rows / NULLIF(calls, 0) AS avg_rows,\n       shared_blks_hit,\n       shared_blks_read,\n       round(shared_blks_hit * 100.0 / NULLIF(shared_blks_hit + shared_blks_read, 0), 2) AS cache_hit_pct,\n       blk_read_time, blk_write_time\nFROM pg_stat_statements\nWHERE calls > 5\nORDER BY total_exec_time DESC LIMIT 20;\n\n-- TOP SQL by 平均执行时间（单次最慢）\nSELECT left(query, 150) AS query, calls,\n       round(mean_exec_time::numeric, 2) AS mean_ms\nFROM pg_stat_statements\nWHERE calls > 10 AND mean_exec_time > 1000\nORDER BY mean_exec_time DESC LIMIT 20;\n\n-- 重置统计（慎用，会清空历史）\n-- SELECT pg_stat_statements_reset();\n```\n\n## 第二步：获取执行计划\n\n### 调用 `explain_query` skill\n\n```sql\n-- 基础执行计划（不执行）\nEXPLAIN SELECT * FROM orders o\nJOIN customers c ON o.customer_id = c.id\nWHERE o.created_at > now() - interval '7 days'\n  AND c.country = 'CN';\n\n-- 实际执行计划（会真实执行）\nEXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)\nSELECT * FROM orders o\nJOIN customers c ON o.customer_id = c.id\nWHERE o.created_at > now() - interval '7 days'\n  AND c.country = 'CN';\n```\n\n**关键指标解读**：\n\n| 指标 | 含义 | 问题信号 |\n|------|------|----------|\n| rows 估算 vs actual rows | 统计信息准确度 | 差距 >10x 说明统计陈旧 |\n| Seq Scan on large table | 全表扫描 | 缺少合适索引 |\n| cost | 优化器代价估算 | 参考 enable_seqscan 等参数 |\n| Buffers: hit/read | 缓存命中 | read 多说明 shared_buffers 不足 |\n| actual time | 实际耗时（ms） | 与 cost 对比判断统计准确度 |\n\n## 第三步：分析统计信息\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 检查列统计信息\nSELECT attname, n_distinct, correlation,\n       most_common_vals, most_common_freqs\nFROM pg_stats\nWHERE tablename = 'orders' AND attname IN ('status','customer_id','created_at');\n\n-- 更新统计信息\nANALYZE orders;\n\n-- 提高高基数列的统计精度（默认 100，范围 1-10000）\nALTER TABLE orders ALTER COLUMN customer_id SET STATISTICS 500;\nANALYZE orders (customer_id);\n```\n\n## 第四步：索引优化\n\n### 调用 `get_table_stats` skill\n\n```sql\n-- 检查现有索引\nSELECT indexname, indexdef,\n       idx_scan, idx_tup_read, idx_tup_fetch,\n       pg_size_pretty(pg_relation_size(indexrelid)) AS size\nFROM pg_stat_user_indexes\nWHERE relname = 'orders'\nORDER BY idx_scan DESC;\n\n-- 创建合适索引\nCREATE INDEX CONCURRENTLY idx_orders_created_at ON orders(created_at DESC);\nCREATE INDEX CONCURRENTLY idx_orders_customer_status ON orders(customer_id, status) INCLUDE (amount);\n\n-- 表达式索引（针对函数查询）\nCREATE INDEX CONCURRENTLY idx_orders_date ON orders(date_trunc('day', created_at));\n\n-- GIN 索引（针对 JSONB 或全文检索）\nCREATE INDEX CONCURRENTLY idx_orders_meta ON orders USING GIN(metadata);\n```\n\n## 第五步：查询改写建议\n\n```sql\n-- 避免 SELECT *，只查询需要的列\n-- 差：SELECT * FROM large_table WHERE id = 1;\n-- 好：SELECT id, name, status FROM large_table WHERE id = 1;\n\n-- 避免对索引列使用函数（会导致索引失效）\n-- 差：WHERE UPPER(email) = 'TEST@EXAMPLE.COM'\n-- 好：WHERE email = lower('test@example.com')  或使用函数索引\n\n-- 使用 EXISTS 替代 IN（子查询场景）\n-- 差：WHERE id IN (SELECT user_id FROM orders WHERE amount > 1000)\n-- 好：WHERE EXISTS (SELECT 1 FROM orders WHERE user_id = user.id AND amount > 1000)\n\n-- 批量更新用 CTE\nWITH to_update AS (\n  SELECT id FROM orders WHERE status = 'pending' AND created_at < now() - interval '30 days'\n  LIMIT 10000\n)\nUPDATE orders SET status = 'expired'\nWHERE id IN (SELECT id FROM to_update);\n```\n\n## 第六步：配置调优\n\n### 调用 `get_db_variables` skill\n\n```sql\nSELECT name, setting, unit FROM pg_settings\nWHERE name IN (\n  'enable_seqscan','enable_hashjoin','enable_mergejoin',\n  'random_page_cost','seq_page_cost','cpu_tuple_cost',\n  'join_collapse_limit','from_collapse_limit'\n);\n\n-- SSD 存储建议降低 random_page_cost\nSET random_page_cost = 1.1;\n```",
    },
    {
        "category": "性能诊断",
        "title": "PostgreSQL 写入慢诊断优化流程",
        "content": "# PostgreSQL 写入慢诊断优化流程\n\n## 概述\n\nPostgreSQL 写入慢（INSERT/UPDATE/DELETE 延迟高）通常由：磁盘 IO 瓶颈、WAL 写入压力、检查点风暴、锁竞争、索引过多、触发器约束开销等原因引起。\n\n## 第一步：确认 IO 使用情况\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n```bash\niostat -x 1 5\niotop -p $(pgrep -d',' postgres)\n```\n\n关注：`%util` 接近 100% -> 磁盘饱和；`await` > 10ms -> IO 延迟高。\n\n## 第二步：查看当前写入相关等待\n\n### 调用 `get_process_list` skill\n\n```sql\nSELECT pid, usename, wait_event_type, wait_event,\n       now() - query_start AS duration,\n       left(query, 150) AS query\nFROM pg_stat_activity\nWHERE state = 'active'\n  AND wait_event_type IN ('Lock','LWLock','IO')\nORDER BY duration DESC NULLS LAST;\n```\n\n重点关注：\n- `wait_event = 'WALWrite'` -> WAL 写入瓶颈\n- `wait_event = 'DataFileWrite'` -> 数据文件写入慢\n- `wait_event = 'CheckpointWrite'` -> 检查点压力\n- `wait_event_type = 'Lock'` -> 行锁或表锁竞争\n\n## 第三步：检查检查点频率\n\n### 调用 `get_db_status` skill\n\n```sql\nSELECT checkpoints_timed, checkpoints_req,\n       checkpoint_write_time, checkpoint_sync_time,\n       buffers_checkpoint, buffers_clean, buffers_backend,\n       buffers_backend_fsync\nFROM pg_stat_bgwriter;\n```\n\n| 指标 | 告警条件 | 说明 |\n|------|----------|------|\n| checkpoints_req | 持续 > 0 | 检查点被频繁触发，max_wal_size 不足 |\n| buffers_backend_fsync | > 0 | 后端进程自行 fsync，IO 压力极高 |\n| checkpoint_sync_time | > 1000ms | 检查点同步慢，磁盘饱和 |\n\n**调优**：\n```sql\n-- 增大 WAL 缓冲区，减少检查点触发频率\nALTER SYSTEM SET max_wal_size = '4GB';\nALTER SYSTEM SET checkpoint_completion_target = 0.9;\nSELECT pg_reload_conf();\n```\n\n## 第四步：WAL 写入分析\n\n### 调用 `get_db_variables` skill\n\n```sql\nSELECT name, setting, unit FROM pg_settings\nWHERE name IN (\n  'wal_level','wal_sync_method','synchronous_commit',\n  'commit_delay','wal_buffers','full_page_writes',\n  'wal_compression','max_wal_size','min_wal_size'\n);\n```\n\n**优化建议**：\n```sql\n-- 对可接受少量数据丢失的场景，关闭同步提交（性能提升 50%+）\nALTER SYSTEM SET synchronous_commit = 'off';\n\n-- 开启 WAL 压缩（CPU 换 IO）\nALTER SYSTEM SET wal_compression = 'on';\n\n-- 增大 WAL 缓冲区（默认 shared_buffers 的 1/32，最大 16MB）\nALTER SYSTEM SET wal_buffers = '16MB';\nSELECT pg_reload_conf();\n```\n\n## 第五步：锁竞争分析\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 查看当前锁等待\nSELECT blocked.pid, blocked.usename,\n       blocking.pid AS blocking_pid,\n       now() - blocked.query_start AS wait_duration,\n       blocked.query AS waiting_query,\n       blocking.query AS blocking_query\nFROM pg_stat_activity blocked\nJOIN pg_stat_activity blocking ON blocking.pid = ANY(pg_blocking_pids(blocked.pid))\nWHERE cardinality(pg_blocking_pids(blocked.pid)) > 0;\n\n-- 查看表级锁\nSELECT relation::regclass, mode, granted, pid\nFROM pg_locks\nWHERE relation IS NOT NULL AND NOT granted\nORDER BY relation;\n```\n\n## 第六步：写入优化建议\n\n```sql\n-- 批量插入替代单行插入\nINSERT INTO orders (customer_id, amount, status)\nSELECT customer_id, amount, 'pending'\nFROM staging_orders;\n\n-- 写入前临时禁用索引（批量导入场景）\nALTER INDEX idx_name DISABLE; -- PG 无此语法，用 REINDEX 后替代\n-- 推荐：先 COPY，后 CREATE INDEX CONCURRENTLY\n\n-- 使用 COPY 替代大量 INSERT（速度可提升 10~100x）\nCOPY orders FROM '/path/to/data.csv' WITH (FORMAT csv, HEADER true);\n\n-- 减少不必要的索引（每个写操作都需要维护所有索引）\nDROP INDEX CONCURRENTLY idx_unused;\n\n-- 禁用不需要的触发器（批量操作期间）\nALTER TABLE orders DISABLE TRIGGER trigger_name;\n-- 操作完成后重新启用\nALTER TABLE orders ENABLE TRIGGER trigger_name;\n```",
    },
    {
        "category": "性能诊断",
        "title": "PostgreSQL 索引优化诊断流程",
        "content": "# PostgreSQL 索引优化诊断流程\n\n## 概述\n\n索引是 PostgreSQL 性能优化的核心手段。不合理的索引（缺失、冗余、失效、膨胀）会同时导致查询慢和写入慢。本文档提供全面的索引健康诊断与优化流程。\n\n## 第一步：索引使用情况总览\n\n### 调用 `get_table_stats` skill\n\n```sql\n-- 各表索引使用统计\nSELECT s.schemaname, s.relname AS table_name,\n       s.seq_scan, s.idx_scan,\n       CASE WHEN s.seq_scan + s.idx_scan > 0\n            THEN round(s.idx_scan * 100.0 / (s.seq_scan + s.idx_scan), 2)\n            ELSE 0 END AS idx_scan_pct,\n       s.n_live_tup,\n       pg_size_pretty(pg_total_relation_size(s.relid)) AS total_size\nFROM pg_stat_user_tables s\nWHERE s.n_live_tup > 10000\nORDER BY s.seq_scan DESC LIMIT 30;\n```\n\n## 第二步：发现未使用的索引\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 从未被使用的索引（排除主键）\nSELECT schemaname, relname AS table_name,\n       indexrelname AS index_name,\n       idx_scan,\n       pg_size_pretty(pg_relation_size(indexrelid)) AS index_size\nFROM pg_stat_user_indexes\nWHERE idx_scan = 0\n  AND schemaname NOT IN ('pg_catalog','information_schema')\n  AND indexrelname NOT LIKE '%_pkey'\nORDER BY pg_relation_size(indexrelid) DESC;\n\n-- 很少使用的索引（idx_scan < 100 但存在超过 7 天）\nSELECT schemaname, relname, indexrelname, idx_scan,\n       pg_size_pretty(pg_relation_size(indexrelid)) AS index_size\nFROM pg_stat_user_indexes\nWHERE idx_scan < 100\n  AND schemaname NOT IN ('pg_catalog','information_schema')\nORDER BY pg_relation_size(indexrelid) DESC LIMIT 20;\n```\n\n## 第三步：发现重复/冗余索引\n\n```sql\n-- 查找重复索引（相同列的不同索引）\nSELECT a.indrelid::regclass AS table_name,\n       a.indexrelid::regclass AS index_a,\n       b.indexrelid::regclass AS index_b,\n       pg_size_pretty(pg_relation_size(a.indexrelid)) AS size_a\nFROM pg_index a\nJOIN pg_index b ON a.indrelid = b.indrelid\n  AND a.indexrelid < b.indexrelid\n  AND a.indkey = b.indkey\nWHERE a.indrelid IN (\n  SELECT relid FROM pg_stat_user_tables\n);\n```\n\n## 第四步：发现缺失的索引\n\n```sql\n-- 高频顺序扫描的大表（可能缺少索引）\nSELECT relname, seq_scan, idx_scan, n_live_tup,\n       seq_tup_read / NULLIF(seq_scan, 0) AS avg_tup_per_scan\nFROM pg_stat_user_tables\nWHERE seq_scan > 100 AND n_live_tup > 10000\nORDER BY seq_tup_read DESC LIMIT 20;\n```\n\n结合 `explain_query` skill 对具体慢查询分析，判断是否需要新建索引。\n\n## 第五步：索引膨胀检查\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 使用 pgstattuple 检查索引膨胀（需安装扩展）\nSELECT * FROM pgstattuple('idx_orders_customer_id');\n\n-- 简单估算：索引大小与表大小比例\nSELECT relname,\n       pg_size_pretty(pg_relation_size(relid)) AS table_size,\n       pg_size_pretty(pg_indexes_size(relid)) AS indexes_size,\n       round(pg_indexes_size(relid) * 100.0 / NULLIF(pg_relation_size(relid), 0), 2) AS idx_to_table_pct\nFROM pg_stat_user_tables\nORDER BY pg_indexes_size(relid) DESC LIMIT 20;\n```\n\n## 第六步：索引类型选择\n\n| 索引类型 | 适用场景 | 示例 |\n|----------|----------|------|\n| B-tree | 默认，等值/范围查询 | CREATE INDEX ON t(col) |\n| GIN | 数组、JSONB、全文检索 | CREATE INDEX ON t USING GIN(tags) |\n| GiST | 几何、范围类型 | CREATE INDEX ON t USING GIST(location) |\n| BRIN | 超大顺序写入表（时序数据） | CREATE INDEX ON t USING BRIN(created_at) |\n| Hash | 纯等值查询 | CREATE INDEX ON t USING HASH(id) |\n\n```sql\n-- 覆盖索引（INCLUDE，避免回表）\nCREATE INDEX CONCURRENTLY idx_orders_covering\n  ON orders(customer_id, status)\n  INCLUDE (amount, created_at);\n\n-- 部分索引（减小索引大小）\nCREATE INDEX CONCURRENTLY idx_orders_active\n  ON orders(created_at)\n  WHERE status = 'active';\n\n-- 重建膨胀索引（在线）\nREINDEX INDEX CONCURRENTLY idx_name;\n\n-- 删除无用索引（在线）\nDROP INDEX CONCURRENTLY idx_unused_name;\n```\n\n## 第七步：索引维护计划\n\n1. 每周检查一次未使用索引（pg_stat_user_indexes.idx_scan = 0）\n2. 每月检查索引膨胀率，超过 30% 时 REINDEX CONCURRENTLY\n3. 新建索引时使用 CONCURRENTLY，避免锁表\n4. 数据库重启后 pg_stat 重置，至少运行 24h 后再评估 idx_scan",
    },
    {
        "category": "故障排查",
        "title": "PostgreSQL 死锁诊断优化流程",
        "content": "# PostgreSQL 死锁诊断优化流程\n\n## 概述\n\nPostgreSQL 死锁（Deadlock）发生时，数据库会自动检测并终止代价较低的一方事务，同时记录 ERROR 日志。需要分析死锁模式并优化事务逻辑。\n\n## 第一步：确认死锁发生\n\n### 调用 `get_db_status` skill\n\n```sql\n-- 查看死锁统计\nSELECT datname, deadlocks, conflicts, temp_files, temp_bytes\nFROM pg_stat_database\n```\n\ndead_locks 计数持续增长说明死锁频发。\n\n### 查看日志中的死锁记录\n\n```bash\n# postgresql.conf 配置死锁日志\n# log_lock_waits = on\n# deadlock_timeout = 1s\n# log_min_duration_statement = 0\n\ngrep -i deadlock $PGDATA/log/postgresql*.log | tail -50\n```\n\n## 第二步：实时监控锁等待\n\n### 调用 `get_process_list` skill\n\n```sql\n-- 查看所有锁等待关系\nSELECT blocked.pid AS blocked_pid,\n       blocked.usename AS blocked_user,\n       blocked.application_name,\n       now() - blocked.query_start AS blocked_for,\n       blocked.query AS blocked_query,\n       blocking.pid AS blocking_pid,\n       blocking.usename AS blocking_user,\n       blocking.query AS blocking_query\nFROM pg_stat_activity blocked\nJOIN pg_stat_activity blocking\n  ON blocking.pid = ANY(pg_blocking_pids(blocked.pid))\nWHERE cardinality(pg_blocking_pids(blocked.pid)) > 0\nORDER BY blocked_for DESC;\n```\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 详细锁信息\nSELECT l.pid, l.locktype, l.relation::regclass AS relation,\n       l.mode, l.granted,\n       a.usename, a.state,\n       left(a.query, 100) AS query\nFROM pg_locks l\nJOIN pg_stat_activity a ON l.pid = a.pid\nWHERE NOT l.granted\nORDER BY l.relation;\n\n-- 检查长时间运行的事务（死锁根源）\nSELECT pid, usename, application_name,\n       xact_start,\n       now() - xact_start AS txn_duration,\n       state,\n       left(query, 150) AS current_query\nFROM pg_stat_activity\nWHERE xact_start IS NOT NULL\n  AND now() - xact_start > interval '30 seconds'\nORDER BY txn_duration DESC;\n```\n\n## 第三步：死锁模式分析\n\n**经典死锁场景**：\n\n| 场景 | 事务 A | 事务 B |\n|------|--------|--------|\n| 顺序相反 | UPDATE t WHERE id=1; UPDATE t WHERE id=2 | UPDATE t WHERE id=2; UPDATE t WHERE id=1 |\n| 外键死锁 | INSERT child; UPDATE parent | UPDATE parent; INSERT child |\n| 索引页锁 | 逆序插入相邻行 | 顺序插入相邻行 |\n\n## 第四步：解决方案\n\n### 应急处理\n\n```sql\n-- 查找并终止阻塞进程\nSELECT pg_terminate_backend(blocking_pid)\nFROM (\n  SELECT UNNEST(pg_blocking_pids(pid)) AS blocking_pid\n  FROM pg_stat_activity\n  WHERE cardinality(pg_blocking_pids(pid)) > 0\n) t;\n```\n\n### 预防措施\n\n1. **统一加锁顺序**：应用层保证始终按相同顺序操作多行\n\n```sql\n-- 差：两个事务以不同顺序更新\n-- 好：先对操作的行排序，再按序更新\nUPDATE accounts SET balance = balance - 100\nWHERE id IN (SELECT id FROM accounts WHERE id IN (1,2) ORDER BY id)\n  AND id = 1;\n```\n\n2. **缩短事务持有锁时间**：减少事务内的外部调用和耗时操作\n\n3. **使用 SELECT FOR UPDATE SKIP LOCKED**（队列场景）\n\n```sql\n-- 避免争抢锁的队列消费模式\nBEGIN;\nSELECT * FROM job_queue\nWHERE status = 'pending'\nORDER BY id\nLIMIT 1\nFOR UPDATE SKIP LOCKED;\n-- 处理任务...\nUPDATE job_queue SET status = 'done' WHERE id = :id;\nCOMMIT;\n```\n\n4. **配置死锁超时**\n\n```sql\n-- 会话级设置（防止长时间等待）\nSET lock_timeout = '5s';\nSET deadlock_timeout = '500ms';\n\n-- 全局配置（postgresql.conf）\n-- lock_timeout = '10s'\n-- deadlock_timeout = '1s'\n```\n\n5. **开启锁等待日志**\n\n```sql\nALTER SYSTEM SET log_lock_waits = on;\nALTER SYSTEM SET deadlock_timeout = '1s';\nSELECT pg_reload_conf();\n```",
    },
    {
        "category": "故障排查",
        "title": "PostgreSQL 连接失败诊断流程",
        "content": "# PostgreSQL 连接失败诊断流程\n\n## 概述\n\nPostgreSQL 连接失败的常见原因：连接数耗尽、认证失败、pg_hba.conf 拒绝、网络不通、数据库未启动、SSL 问题等。\n\n## 第一步：确认数据库服务状态\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n```bash\n# 检查 PostgreSQL 进程\npg_ctl status -D $PGDATA\nps aux | grep postgres | grep -v grep\n\n# 检查端口监听\nss -tlnp | grep 5432\nnetstat -tlnp | grep 5432\n\n# 检查系统日志\njournalctl -u postgresql --since '1 hour ago' | tail -50\n```\n\n## 第二步：检查连接数是否耗尽\n\n### 调用 `get_db_status` skill\n\n```sql\n-- 连接数使用情况\nSELECT count(*) AS used,\n       (SELECT setting::int FROM pg_settings WHERE name = 'max_connections') AS max,\n       (SELECT setting::int FROM pg_settings WHERE name = 'superuser_reserved_connections') AS reserved\nFROM pg_stat_activity;\n\n-- 各数据库连接分布\nSELECT datname, count(*) AS connections\nFROM pg_stat_activity\nGROUP BY datname ORDER BY connections DESC;\n\n-- 各用户连接分布\nSELECT usename, count(*) AS connections, state\nFROM pg_stat_activity\nGROUP BY usename, state ORDER BY connections DESC;\n\n-- 是否有连接泄漏（idle 连接时间很长）\nSELECT pid, usename, application_name, client_addr, state,\n       now() - state_change AS idle_duration\nFROM pg_stat_activity\nWHERE state = 'idle'\n  AND now() - state_change > interval '1 hour'\nORDER BY idle_duration DESC LIMIT 20;\n```\n\n## 第三步：检查认证配置\n\n### 调用 `get_os_metrics` skill\n\n```bash\n# 查看 pg_hba.conf\ncat $PGDATA/pg_hba.conf | grep -v '^#' | grep -v '^$'\n\n# 查看最近的认证失败日志\ngrep -i 'authentication failed\\|no pg_hba.conf entry\\|password authentication' \\\n  $PGDATA/log/postgresql*.log | tail -30\n\n# 查看 PostgreSQL 日志目录\npsql -c \"SHOW log_directory;\"\npsql -c \"SHOW data_directory;\"\n```\n\n**常见 pg_hba.conf 错误**：\n\n| 错误信息 | 原因 | 解决 |\n|----------|------|------|\n| no pg_hba.conf entry | 客户端 IP 未授权 | 添加对应规则 |\n| password authentication failed | 密码错误 | 重置密码 |\n| peer authentication failed | 本地连接用户名不匹配 | 改为 md5 或 scram-sha-256 |\n| SSL required | 客户端未开启 SSL | 添加 sslmode=require |\n\n## 第四步：连接数配置优化\n\n### 调用 `get_db_variables` skill\n\n```sql\nSELECT name, setting FROM pg_settings\nWHERE name IN (\n  'max_connections','superuser_reserved_connections',\n  'listen_addresses','port'\n);\n```\n\n**临时释放连接**：\n\n```sql\n-- 终止 idle 连接（保留 superuser 连接用于操作）\nSELECT pg_terminate_backend(pid)\nFROM pg_stat_activity\nWHERE state = 'idle'\n  AND usename != 'postgres'\n  AND now() - state_change > interval '10 minutes';\n\n-- 终止特定应用的连接\nSELECT pg_terminate_backend(pid)\nFROM pg_stat_activity\nWHERE application_name = 'old_app_version';\n```\n\n## 第五步：部署连接池\n\n推荐使用 PgBouncer 管理连接：\n\n```ini\n; pgbouncer.ini 关键配置\n[pgbouncer]\npool_mode = transaction          ; transaction 模式性能最好\nmax_client_conn = 2000           ; 客户端最大连接数\ndefault_pool_size = 20           ; 每个 user/database 对的连接池大小\nmin_pool_size = 5                ; 最小维持连接数\nreserve_pool_size = 5            ; 备用连接数\nreserve_pool_timeout = 3         ; 等待备用连接超时（秒）\nserver_idle_timeout = 600        ; 空闲服务器连接超时（秒）\nmax_db_connections = 50          ; 每个数据库最大连接数\n```\n\n**连接数规划建议**：\n- PostgreSQL max_connections = (CPU核数 x 4) + 并发活跃连接数上限\n- 一般不超过 500，超过建议上 PgBouncer\n- superuser_reserved_connections = 3（保留 DBA 紧急操作通道）",
    },
    {
        "category": "故障排查",
        "title": "PostgreSQL SQL执行失败诊断流程",
        "content": "# PostgreSQL SQL执行失败诊断流程\n\n## 概述\n\nPostgreSQL SQL 执行失败的常见原因：语法错误、权限不足、约束违反、类型不匹配、资源耗尽（内存/磁盘）、超时、事务冲突等。\n\n## 第一步：收集错误信息\n\n### 调用 `get_os_metrics` skill（查看日志）\n\n```bash\ntail -100 $PGDATA/log/$(ls -t $PGDATA/log/ | head -1)\ngrep -E 'ERROR|FATAL|PANIC' $PGDATA/log/postgresql*.log | tail -50\n```\n\n### 调用 `get_db_variables` skill\n\n```sql\nSELECT name, setting FROM pg_settings\nWHERE name IN (\n  'log_min_error_statement','log_min_duration_statement',\n  'log_error_verbosity','log_destination','log_directory'\n);\n```\n\n## 第二步：常见错误类型诊断\n\n### 2.1 权限错误（permission denied）\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\nSELECT grantee, table_schema, table_name, privilege_type\nFROM information_schema.role_table_grants\nWHERE table_name = 'your_table' ORDER BY grantee;\n\nSELECT nspname AS schema,\n       has_schema_privilege('your_user', nspname, 'USAGE') AS can_use\nFROM pg_namespace WHERE nspname NOT LIKE 'pg_%';\n\n-- 授予权限\nGRANT SELECT, INSERT, UPDATE, DELETE ON your_table TO your_user;\nGRANT USAGE ON SCHEMA public TO your_user;\n```\n\n### 2.2 约束违反（constraint violation）\n\n```sql\nSELECT conname, contype, pg_get_constraintdef(oid) AS definition\nFROM pg_constraint\nWHERE conrelid = 'your_table'::regclass ORDER BY contype;\n\n-- unique_violation (23505): 唯一键冲突 -> 使用 ON CONFLICT\nINSERT INTO t(id, val) VALUES (1, 'x')\nON CONFLICT (id) DO UPDATE SET val = EXCLUDED.val;\n\n-- foreign_key_violation (23503): 先插入父表记录\n-- not_null_violation (23502): 检查应用传参是否有 null\n-- check_violation (23514): 查看 CHECK 约束定义\n```\n\n### 2.3 超时错误\n\n```sql\nSELECT name, setting, unit FROM pg_settings\nWHERE name IN ('statement_timeout','lock_timeout','idle_in_transaction_session_timeout');\n\n-- 会话级临时调整\nSET statement_timeout = '60s';\nSET lock_timeout = '10s';\nSET idle_in_transaction_session_timeout = '5min';\n```\n\n### 2.4 磁盘空间不足\n\n```sql\nSELECT datname, temp_files, pg_size_pretty(temp_bytes) AS temp_size\nFROM pg_stat_database WHERE temp_bytes > 0;\n\nSELECT spcname, pg_size_pretty(pg_tablespace_size(spcname)) AS size\nFROM pg_tablespace;\n```\n\n### 2.5 内存不足（out of memory）\n\n```sql\nSET work_mem = '16MB';\n\nSELECT name, setting, unit FROM pg_settings\nWHERE name IN ('shared_buffers','work_mem','maintenance_work_mem','temp_buffers');\n```\n\n## 第三步：事务冲突（serialization failure）\n\n```sql\n-- 错误码 40001: could not serialize access due to concurrent update\n-- 错误码 40P01: deadlock detected\n-- 应用层需实现重试逻辑（指数退避）\n\nSHOW transaction_isolation;\nSET default_transaction_isolation = 'read committed';\n```\n\n## 第四步：PostgreSQL 错误码速查\n\n| 错误码 | 含义 | 处理方向 |\n|--------|------|----------|\n| 23505 | unique_violation | ON CONFLICT 或应用去重 |\n| 23503 | foreign_key_violation | 确保父记录存在 |\n| 23502 | not_null_violation | 检查应用传参 |\n| 42601 | syntax_error | 检查 SQL 语法 |\n| 42P01 | undefined_table | 确认表名和 schema |\n| 42501 | insufficient_privilege | GRANT 相应权限 |\n| 53100 | disk_full | 清理磁盘空间 |\n| 40001 | serialization_failure | 应用重试 |\n| 40P01 | deadlock_detected | 优化事务顺序 |\n| 57014 | query_canceled | 检查 statement_timeout |\n\n## 第五步：开启详细错误日志\n\n```sql\nALTER SYSTEM SET log_min_duration_statement = 1000;  -- 记录超过1s的SQL\nALTER SYSTEM SET log_min_error_statement = 'error';  -- 记录错误SQL\nALTER SYSTEM SET log_error_verbosity = 'verbose';    -- 详细错误信息\nSELECT pg_reload_conf();\n```",
    },
    {
        "category": "故障排查",
        "title": "PostgreSQL 主备延时诊断流程",
        "content": "# PostgreSQL 主备延时诊断流程\n\n## 概述\n\nPostgreSQL 流复制延迟（replication lag）会导致备库数据不及时，影响读写分离的读一致性。主备延迟超过阈值时需立即排查。\n\n## 第一步：确认复制延迟\n\n### 调用 `get_replication_status` skill\n\n```sql\n-- 在主库执行\nSELECT client_addr, application_name, state,\n       sent_lsn, write_lsn, flush_lsn, replay_lsn,\n       pg_size_pretty(sent_lsn - replay_lsn) AS total_lag_bytes,\n       write_lag, flush_lag, replay_lag\nFROM pg_stat_replication\nORDER BY replay_lag DESC NULLS LAST;\n\n-- 在备库执行，查看本地延迟\nSELECT now() - pg_last_xact_replay_timestamp() AS replication_delay;\nSELECT pg_is_in_recovery(), pg_last_wal_receive_lsn(), pg_last_wal_replay_lsn();\n```\n\n| 指标 | 说明 | 告警阈值 |\n|------|------|----------|\n| replay_lag | 主库发送到备库重放的时间差 | > 60s |\n| sent_lsn - replay_lsn | 字节差 | > 1GB |\n| pg_last_xact_replay_timestamp | 备库最后重放事务时间 | > 5min ago |\n\n## 第二步：诊断延迟原因\n\n### 2.1 网络带宽不足\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n```bash\n# 检查主备网络带宽使用\nifstat -i eth0 1 5\nping -c 10 <备库IP>  # 检查网络延迟和丢包\n```\n\n### 2.2 备库 CPU/IO 不足\n\n```bash\n# 在备库检查 WAL receiver 和 startup 进程资源\nps aux | grep 'wal receiver\\|startup'\niostat -x 1 5\n```\n\n### 2.3 主库写入量过大（WAL 生成速度超过备库重放速度）\n\n### 调用 `get_db_status` skill（在主库执行）\n\n```sql\n-- 检查 WAL 生成速率\nSELECT pg_current_wal_lsn(),\n       pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), '0/0')) AS total_wal_generated;\n\n-- 查看高频写入表\nSELECT relname, n_tup_ins, n_tup_upd, n_tup_del,\n       n_tup_ins + n_tup_upd + n_tup_del AS total_writes\nFROM pg_stat_user_tables\nORDER BY total_writes DESC LIMIT 20;\n```\n\n### 2.4 备库有长查询阻塞 WAL 重放\n\n```sql\n-- 在备库执行\nSELECT pid, usename, application_name,\n       now() - query_start AS duration,\n       wait_event_type, wait_event,\n       left(query, 150) AS query\nFROM pg_stat_activity\nWHERE state = 'active'\nORDER BY duration DESC NULLS LAST;\n\n-- 检查 hot_standby_feedback\nSELECT name, setting FROM pg_settings\nWHERE name IN ('hot_standby_feedback','max_standby_streaming_delay',\n               'recovery_min_apply_delay','wal_receiver_timeout');\n```\n\n### 2.5 复制槽堆积\n\n```sql\n-- 在主库检查复制槽\nSELECT slot_name, slot_type, active, restart_lsn,\n       pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) AS lag\nFROM pg_replication_slots\nORDER BY pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn) DESC;\n```\n\n## 第三步：优化方案\n\n### 减少备库查询对重放的影响\n\n```sql\n-- postgresql.conf (备库)\n-- max_standby_streaming_delay = 30s  -- 允许备库查询延迟重放的最大时间\n-- hot_standby_feedback = on          -- 备库将事务信息反馈主库，防止 vacuum 删除备库需要的行\nALTER SYSTEM SET max_standby_streaming_delay = '30s';\nALTER SYSTEM SET hot_standby_feedback = on;\nSELECT pg_reload_conf();\n```\n\n### 优化 WAL 传输\n\n```sql\n-- 开启 WAL 压缩减少网络传输量\nALTER SYSTEM SET wal_compression = 'on';\n\n-- 增大 wal_sender 缓冲区\nALTER SYSTEM SET wal_sender_timeout = '60s';\nALTER SYSTEM SET wal_receiver_timeout = '60s';\nSELECT pg_reload_conf();\n```\n\n### 主库写入优化\n\n1. 减少不必要的全量 WAL 写入（检查 wal_level，replica 即可）\n2. 批量写入替代频繁小事务\n3. 评估是否需要增加备库硬件配置（SSD、更多 CPU）",
    },
    {
        "category": "故障排查",
        "title": "PostgreSQL 主备数据不一致诊断流程",
        "content": "# PostgreSQL 主备数据不一致诊断流程\n\n## 概述\n\nPostgreSQL 主备数据不一致是严重问题，通常发生在：强制 failover 后、复制中断期间发生写入、硬件故障、人为误操作等场景。\n\n## 第一步：确认主备状态\n\n### 调用 `get_replication_status` skill\n\n```sql\n-- 在主库执行\nSELECT client_addr, application_name, state,\n       sent_lsn, flush_lsn, replay_lsn,\n       pg_size_pretty(sent_lsn - replay_lsn) AS lag_bytes\nFROM pg_stat_replication;\n\n-- 确认主库角色\nSELECT pg_is_in_recovery(); -- false = 主库\n\n-- 在备库执行\nSELECT pg_is_in_recovery(); -- true = 备库\nSELECT pg_last_wal_receive_lsn(), pg_last_wal_replay_lsn(),\n       now() - pg_last_xact_replay_timestamp() AS replication_delay;\n```\n\n## 第二步：检查数据差异\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 比较主备关键表的行数（在各自执行）\nSELECT schemaname, tablename,\n       (xpath('/row/cnt/text()',\n              query_to_xml('SELECT count(*) AS cnt FROM '||schemaname||'.'||tablename, false, true, '')))[1]::text::int AS row_count\nFROM pg_tables\nWHERE schemaname NOT IN ('pg_catalog','information_schema')\nORDER BY row_count DESC LIMIT 20;\n\n-- 检查系统目录是否一致\nSELECT count(*) FROM pg_class WHERE relkind = 'r';\nSELECT count(*) FROM pg_index;\n```\n\n## 第三步：分析不一致原因\n\n### 3.1 复制中断期间主库有写入\n\n```bash\n# 检查 PostgreSQL 日志中的复制中断记录\ngrep -E 'replication|standby|recovery' $PGDATA/log/postgresql*.log | \\\n  grep -E 'disconnect|terminate|error|fail' | tail -50\n```\n\n### 3.2 使用了 synchronous_commit = off 且主库崩溃\n\n```sql\nSELECT name, setting FROM pg_settings\nWHERE name = 'synchronous_commit';\n```\n\n如果 synchronous_commit = off 且主库在提交后崩溃前未将 WAL 刷到磁盘，备库可能丢失这部分数据。\n\n### 3.3 时间线分叉（Timeline divergence）\n\n```bash\n# 检查两个节点的时间线历史\npg_controldata $PGDATA | grep 'Timeline ID\\|Latest checkpoint'\n\n# 检查 pg_wal 目录的时间线文件\nls $PGDATA/pg_wal/*.history 2>/dev/null\n```\n\n## 第四步：修复方案\n\n### 方案一：重新同步备库（推荐）\n\n```bash\n# 停止备库\npg_ctl stop -D $PGDATA_STANDBY\n\n# 使用 pg_basebackup 重新构建备库\npg_basebackup -h primary_host -U replication_user \\\n  -D $PGDATA_STANDBY_NEW \\\n  -P -Xs -R --checkpoint=fast\n\n# 启动新备库\npg_ctl start -D $PGDATA_STANDBY_NEW\n```\n\n### 方案二：使用 pg_rewind 追上主库\n\n```bash\n# pg_rewind 要求主库开启 wal_log_hints = on 或使用 checksums\npg_rewind --target-pgdata=$PGDATA_STANDBY \\\n  --source-server=\"host=primary_host user=postgres dbname=postgres\" \\\n  -P\n```\n\n### 方案三：逻辑比较和修复（数据差异小时）\n\n```bash\n# 使用 pg_diff_tables 或手动比较关键表\n# 导出主库数据\npg_dump -t critical_table -F c -f /tmp/critical_table.dump primary_db\n\n# 在备库（已提升为主库）中恢复\npg_restore -d target_db /tmp/critical_table.dump\n```\n\n## 第五步：预防措施\n\n```sql\n-- 使用同步复制（强一致性，有性能影响）\nALTER SYSTEM SET synchronous_standby_names = 'standby1';\nALTER SYSTEM SET synchronous_commit = 'remote_apply';\nSELECT pg_reload_conf();\n\n-- 开启数据校验和（新建集群时）\n-- initdb --data-checksums\n\n-- 启用 wal_log_hints（支持 pg_rewind）\nALTER SYSTEM SET wal_log_hints = on;\n\n-- 定期验证备库数据\n-- 通过监控系统对比主备关键表行数\n```",
    },
    {
        "category": "故障排查",
        "title": "PostgreSQL 启动失败诊断流程",
        "content": "# PostgreSQL 启动失败诊断流程\n\n## 概述\n\nPostgreSQL 启动失败的常见原因：数据目录损坏、控制文件损坏、端口占用、权限问题、配置文件错误、WAL 文件缺失等。\n\n## 第一步：查看错误信息\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n```bash\n# 查看启动状态\npg_ctl status -D $PGDATA\n\n# 查看系统日志\njournalctl -u postgresql --since '30 minutes ago' | tail -100\n\n# 查看 PostgreSQL 日志（最新）\nls -lt $PGDATA/log/ | head -5\ntail -200 $PGDATA/log/$(ls -t $PGDATA/log/ | head -1)\n\n# 查看控制文件信息\npg_controldata $PGDATA\n```\n\n## 第二步：常见失败原因诊断\n\n### 2.1 端口已被占用\n\n```bash\nss -tlnp | grep 5432\nlsof -i :5432\n\n# 如果是残留的 postmaster.pid\ncat $PGDATA/postmaster.pid\nkill -0 <pid>  # 检查进程是否存在\n# 如果进程不存在，安全删除\nrm $PGDATA/postmaster.pid\n```\n\n### 2.2 配置文件错误\n\n```bash\n# 检查配置文件语法\npostgres --config-file=$PGDATA/postgresql.conf -C max_connections 2>&1\n\n# 查看最近修改的配置\ndiff $PGDATA/postgresql.conf $PGDATA/postgresql.conf.bak\n\n# 恢复备份配置\ncp $PGDATA/postgresql.conf.bak $PGDATA/postgresql.conf\n```\n\n**常见配置错误**：\n- shared_buffers 超过系统 SHMMAX 限制\n- data_directory 路径不存在或权限不对\n- listen_addresses 绑定了不存在的网络接口\n- archive_command 配置错误导致无法归档\n\n### 2.3 数据目录权限问题\n\n```bash\n# 检查数据目录权限（必须是 700，owner 是 postgres）\nls -la $PGDATA\nstat $PGDATA\n\n# 修复权限\nchown -R postgres:postgres $PGDATA\nchmod 700 $PGDATA\n```\n\n### 2.4 WAL 文件损坏或缺失\n\n```bash\n# 查看 pg_controldata 中的 WAL 位置\npg_controldata $PGDATA | grep -E 'checkpoint|WAL|redo'\n\n# 检查 WAL 文件是否存在\nls $PGDATA/pg_wal/\n\n# 如果 WAL 缺失，且有归档，尝试从归档恢复\n# restore_command = 'cp /archive/%f %p' (postgresql.conf)\n\n# 最后手段：跳过损坏的 WAL（会丢失数据，生产慎用）\npg_resetwal -f $PGDATA\n```\n\n### 2.5 控制文件损坏\n\n```bash\n# 查看控制文件状态\npg_controldata $PGDATA 2>&1 | head -30\n\n# 从备份恢复控制文件\ncp /backup/pg_control $PGDATA/global/pg_control\n\n# 或使用 pg_resetwal 重建控制文件（危险，最后手段）\npg_resetwal --pgdata=$PGDATA\n```\n\n### 2.6 系统资源不足\n\n```bash\n# 检查共享内存配置\nipcs -m\nsysctl kernel.shmmax kernel.shmall\n\n# 如果 shared_buffers 过大，临时降低\n# postgresql.conf: shared_buffers = 128MB\n\n# 检查打开文件数限制\nulimit -n\n# 如果不足，增大\n# /etc/security/limits.conf: postgres soft nofile 65536\n```\n\n## 第三步：从备份恢复\n\n```bash\n# 使用 pg_basebackup 备份进行时间点恢复 (PITR)\nmkdir /var/lib/postgresql/recovery\ntar xzf /backup/base.tar.gz -C /var/lib/postgresql/recovery\n\n# 配置 recovery.conf (PG12 之前) 或 postgresql.conf (PG12+)\ncat >> /var/lib/postgresql/recovery/postgresql.conf << EOF\nrestore_command = 'cp /archive/%f %p'\nrecovery_target_time = '2026-03-21 10:00:00'\nrecovery_target_action = 'promote'\nEOF\n\ntouch /var/lib/postgresql/recovery/recovery.signal\n\n# 启动恢复\npg_ctl start -D /var/lib/postgresql/recovery\n```\n\n## 第四步：启动后验证\n\n```sql\n-- 验证数据库完整性\nSELECT count(*) FROM pg_class;\nSELECT datname, datallowconn FROM pg_database;\n\n-- 检查系统表是否正常\nSELECT tablename FROM pg_tables WHERE schemaname = 'pg_catalog' LIMIT 5;\n\n-- 验证关键用户表\nSELECT count(*) FROM your_critical_table;\n```",
    },
    {
        "category": "故障排查",
        "title": "PostgreSQL 数据丢失恢复方案",
        "content": "# PostgreSQL 数据丢失恢复方案\n\n## 概述\n\nPostgreSQL 数据丢失场景：误删表/数据库、误 DROP、误 TRUNCATE、硬件故障、磁盘损坏。本文档提供各场景的恢复方案，从快到慢排列。\n\n## 第一步：确认丢失范围\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 确认哪些对象丢失\nSELECT table_schema, table_name\nFROM information_schema.tables\nWHERE table_schema NOT IN ('pg_catalog','information_schema')\nORDER BY table_schema, table_name;\n\n-- 检查行数（与预期对比）\nSELECT relname, n_live_tup, n_dead_tup\nFROM pg_stat_user_tables\nORDER BY n_live_tup DESC;\n\n-- 查看最近 DDL 操作（如果开启了 pg_audit 或 DDL 日志）\nSELECT log_time, user_name, command_tag, object_type, object_name\nFROM pg_catalog.pg_audit_log\nWHERE log_time > now() - interval '1 hour'\nORDER BY log_time DESC;\n```\n\n## 第二步：事务回滚（最快，未提交时）\n\n```sql\n-- 如果操作在事务中未提交\nROLLBACK;\n\n-- 保存点恢复\nROLLBACK TO SAVEPOINT before_delete;\n```\n\n## 第三步：从 MVCC 版本恢复（误删行，未 VACUUM）\n\n```sql\n-- PostgreSQL 的 MVCC 保留旧版本行，可通过时间旅行查询\n-- 注意：仅在未 autovacuum 且未 VACUUM 的情况下有效\n\n-- 设置事务 snapshot 为过去时间\nSELECT * FROM your_table\nAS OF SYSTEM TIME '-5 minutes';  -- 仅 CockroachDB 语法\n\n-- PostgreSQL 原生方式：通过 xmin 系统列\nSELECT *, xmin, xmax FROM your_table WHERE xmax != 0;\n```\n\n## 第四步：从 pg_dump 备份恢复\n\n```bash\n# 恢复单表（逻辑备份）\npg_restore -d target_db -t lost_table /backup/full_backup.dump\n\n# 恢复整个数据库\npg_restore -d postgres -C /backup/full_backup.dump\n\n# 从 SQL 格式备份恢复\npsql target_db < /backup/backup.sql\n\n# 仅恢复特定数据\npg_restore --list /backup/full_backup.dump | grep 'TABLE DATA public lost_table'\npg_restore -d target_db -L /tmp/restore_list.txt /backup/full_backup.dump\n```\n\n## 第五步：时间点恢复（PITR）\n\n```bash\n# 使用 pg_basebackup + WAL 归档进行精确时间点恢复\n\n# 1. 停止当前 PostgreSQL\npg_ctl stop -D $PGDATA\n\n# 2. 恢复基础备份\nrm -rf $PGDATA_RECOVERY\ntar xzf /backup/base.tar.gz -C $PGDATA_RECOVERY\n\n# 3. 配置恢复目标时间（在误操作之前1分钟）\ncat >> $PGDATA_RECOVERY/postgresql.conf << 'EOF'\nrestore_command = 'cp /archive/%f %p'\nrecovery_target_time = '2026-03-21 09:59:00'\nrecovery_target_action = 'promote'\nEOF\n\n# 4. 创建恢复信号文件\ntouch $PGDATA_RECOVERY/recovery.signal\n\n# 5. 启动恢复实例\npg_ctl start -D $PGDATA_RECOVERY -l /tmp/recovery.log\n\n# 6. 等待恢复完成\ntail -f /tmp/recovery.log\n\n# 7. 验证数据后，导出需要的数据\npg_dump -h localhost -p 5433 -t lost_table recovered_db > /tmp/recovered_data.sql\n\n# 8. 导入到生产库\npsql production_db < /tmp/recovered_data.sql\n```\n\n## 第六步：从 pg_basebackup 物理备份恢复\n\n```bash\n# 完整物理恢复（适合整库恢复）\npg_basebackup -h backup_host -U replication \\\n  -D $PGDATA_NEW -Fp -Xs -P -R\n```\n\n## 第七步：使用第三方工具\n\n```bash\n# pg_dirtyread: 从已删除行读取数据（需要 pageinspect 扩展）\nCREATE EXTENSION pageinspect;\n\n# WAL 解析工具（wal2json, pg_waldump）\npg_waldump -p $PGDATA/pg_wal -s 0/1234567 -e 0/1234890\n\n# wal2json 逻辑解析（需要 wal_level = logical）\npg_recvlogical -d mydb --slot=recovery_slot \\\n  --start -f - --plugin=wal2json\n```\n\n## 预防措施\n\n```sql\n-- 配置定期备份\n-- crontab: 0 2 * * * pg_dump -Fc mydb > /backup/mydb_$(date +%Y%m%d).dump\n\n-- 开启 WAL 归档（支持 PITR）\nALTER SYSTEM SET archive_mode = on;\nALTER SYSTEM SET archive_command = 'cp %p /archive/%f';\nALTER SYSTEM SET wal_level = 'replica';\n\n-- 保护关键表（限制高危操作）\nREVOKE DROP ON TABLE critical_table FROM public;\n\n-- 误删保护（触发器）\nCREATE RULE no_delete AS ON DELETE TO critical_table DO INSTEAD NOTHING;\n```",
    },
    {
        "category": "配置与会话",
        "title": "PostgreSQL 系统参数配置诊断优化流程",
        "content": "# PostgreSQL 系统参数配置诊断优化流程\n\n## 概述\n\nPostgreSQL 系统参数配置直接影响性能、稳定性和安全性。本文档提供关键参数的诊断方法、推荐值及调优流程。\n\n## 第一步：查看当前关键参数\n\n### 调用 `get_db_variables` skill\n\n```sql\n-- 内存相关参数\nSELECT name, setting, unit,\n       CASE unit\n         WHEN 'B' THEN pg_size_pretty(setting::bigint)\n         WHEN 'kB' THEN pg_size_pretty(setting::bigint * 1024)\n         WHEN '8kB' THEN pg_size_pretty(setting::bigint * 8192)\n         ELSE setting\n       END AS human_readable,\n       short_desc\nFROM pg_settings\nWHERE name IN (\n  'shared_buffers','work_mem','maintenance_work_mem',\n  'effective_cache_size','temp_buffers','wal_buffers',\n  'max_stack_depth'\n);\n\n-- 连接相关\nSELECT name, setting, short_desc FROM pg_settings\nWHERE name IN (\n  'max_connections','superuser_reserved_connections',\n  'listen_addresses','port','tcp_keepalives_idle'\n);\n\n-- WAL 相关\nSELECT name, setting, unit, short_desc FROM pg_settings\nWHERE name IN (\n  'wal_level','fsync','synchronous_commit',\n  'wal_sync_method','wal_buffers','max_wal_size','min_wal_size',\n  'checkpoint_completion_target','checkpoint_timeout',\n  'archive_mode','archive_command','wal_compression'\n);\n\n-- 查询优化器相关\nSELECT name, setting, short_desc FROM pg_settings\nWHERE name IN (\n  'random_page_cost','seq_page_cost','cpu_tuple_cost',\n  'effective_io_concurrency','max_parallel_workers_per_gather',\n  'max_parallel_workers','parallel_tuple_cost'\n);\n```\n\n## 第二步：检查参数来源（哪里被覆盖）\n\n```sql\n-- 查看参数来源（默认/配置文件/会话）\nSELECT name, setting, source, sourcefile, sourceline\nFROM pg_settings\nWHERE source != 'default'\n  AND source != 'override'\nORDER BY source, name;\n\n-- 查看 postgresql.auto.conf（ALTER SYSTEM 写入的参数）\nSELECT * FROM pg_file_settings\nWHERE sourcefile LIKE '%auto.conf%';\n\n-- 查看参数变更是否需要重启\nSELECT name, setting, pending_restart\nFROM pg_settings\nWHERE pending_restart = true;\n```\n\n## 第三步：推荐参数配置\n\n| 参数 | 推荐值 | 说明 |\n|------|--------|------|\n| shared_buffers | 物理内存 25% | 核心缓冲区，通常 8~32GB |\n| effective_cache_size | 物理内存 75% | 优化器估算可用缓存，不实际分配 |\n| work_mem | 16~256MB | 每个排序/哈希操作，总占用=connections x work_mem |\n| maintenance_work_mem | 512MB~2GB | VACUUM、CREATE INDEX 使用 |\n| wal_buffers | 16MB | WAL 写缓冲，通常 shared_buffers 的 1/32 |\n| max_wal_size | 2~8GB | 两次检查点间最大 WAL 大小 |\n| checkpoint_completion_target | 0.9 | 将检查点写入分散到 90% 的时间窗口 |\n| effective_io_concurrency | 200（SSD）/2（HDD） | 并发 IO 请求数 |\n| random_page_cost | 1.1（SSD）/4.0（HDD） | 随机 IO 代价，影响索引选择 |\n| max_connections | 100~300 | 超过建议用 PgBouncer |\n\n## 第四步：动态调整参数\n\n```sql\n-- 在线修改参数（不需要重启）\nALTER SYSTEM SET work_mem = '32MB';\nALTER SYSTEM SET maintenance_work_mem = '512MB';\nALTER SYSTEM SET effective_io_concurrency = 200;\nALTER SYSTEM SET random_page_cost = 1.1;\nSELECT pg_reload_conf();  -- 使配置生效\n\n-- 需要重启的参数（修改后执行重启）\n-- shared_buffers, max_connections, wal_buffers, listen_addresses\nALTER SYSTEM SET shared_buffers = '8GB';  -- 重启后生效\n\n-- 验证参数已生效\nSHOW work_mem;\nSELECT name, setting FROM pg_settings WHERE name = 'work_mem';\n```\n\n## 第五步：针对不同场景的配置模板\n\n### OLTP（高并发事务）\n\n```ini\nmax_connections = 200\nshared_buffers = 8GB\nwork_mem = 16MB\neffective_cache_size = 24GB\nwal_level = replica\nsynchronous_commit = on\ncheckpoint_completion_target = 0.9\nmax_wal_size = 4GB\nrandom_page_cost = 1.1\neffective_io_concurrency = 200\n```\n\n### OLAP（分析查询）\n\n```ini\nmax_connections = 50\nshared_buffers = 16GB\nwork_mem = 256MB\nmaintenance_work_mem = 2GB\neffective_cache_size = 48GB\nmax_parallel_workers_per_gather = 8\nmax_parallel_workers = 16\nrandom_page_cost = 1.1\njit = on\n```",
    },
    {
        "category": "配置与会话",
        "title": "PostgreSQL 会话连接诊断优化流程",
        "content": "# PostgreSQL 会话连接诊断优化流程\n\n## 概述\n\n会话连接问题包括：连接数超限、连接泄漏、长时间 idle in transaction、连接建立慢等。合理管理连接是 PostgreSQL 稳定运行的基础。\n\n## 第一步：当前连接状态分析\n\n### 调用 `get_process_list` skill\n\n```sql\n-- 连接总览\nSELECT state,\n       count(*) AS count,\n       max(now() - state_change) AS max_duration\nFROM pg_stat_activity\nGROUP BY state ORDER BY count DESC;\n\n-- 按应用和用户分组\nSELECT usename, application_name, client_addr,\n       state, count(*) AS conn_count\nFROM pg_stat_activity\nGROUP BY usename, application_name, client_addr, state\nORDER BY conn_count DESC LIMIT 30;\n\n-- 连接利用率\nSELECT count(*) AS used,\n       (SELECT setting::int FROM pg_settings WHERE name = 'max_connections') AS max_connections,\n       round(count(*) * 100.0 / (SELECT setting::int FROM pg_settings WHERE name = 'max_connections'), 1) AS usage_pct\nFROM pg_stat_activity;\n```\n\n## 第二步：识别问题连接\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 长时间 idle in transaction（持有锁，阻塞其他会话）\nSELECT pid, usename, application_name, client_addr,\n       state, xact_start,\n       now() - xact_start AS txn_duration,\n       left(query, 150) AS last_query\nFROM pg_stat_activity\nWHERE state = 'idle in transaction'\n  AND now() - xact_start > interval '5 minutes'\nORDER BY txn_duration DESC;\n\n-- 长时间 idle 连接（连接泄漏）\nSELECT pid, usename, application_name, client_addr,\n       state, now() - state_change AS idle_duration\nFROM pg_stat_activity\nWHERE state = 'idle'\n  AND now() - state_change > interval '30 minutes'\nORDER BY idle_duration DESC LIMIT 20;\n\n-- 等待锁的连接\nSELECT pid, usename, wait_event_type, wait_event,\n       now() - query_start AS wait_duration,\n       left(query, 150) AS query\nFROM pg_stat_activity\nWHERE wait_event_type = 'Lock'\nORDER BY wait_duration DESC;\n\n-- 长时间运行的活跃查询\nSELECT pid, usename, now() - query_start AS duration,\n       state, left(query, 200) AS query\nFROM pg_stat_activity\nWHERE state = 'active'\n  AND now() - query_start > interval '5 minutes'\n  AND pid != pg_backend_pid()\nORDER BY duration DESC;\n```\n\n## 第三步：处理问题连接\n\n```sql\n-- 取消查询（不断开连接）\nSELECT pg_cancel_backend(pid)\nFROM pg_stat_activity\nWHERE state = 'active'\n  AND now() - query_start > interval '10 minutes'\n  AND pid != pg_backend_pid();\n\n-- 终止连接（断开连接）\nSELECT pg_terminate_backend(pid)\nFROM pg_stat_activity\nWHERE state IN ('idle in transaction','idle')\n  AND now() - state_change > interval '1 hour'\n  AND usename != 'postgres';\n\n-- 终止特定用户所有连接\nSELECT pg_terminate_backend(pid)\nFROM pg_stat_activity\nWHERE usename = 'app_user' AND pid != pg_backend_pid();\n\n-- 终止特定数据库所有连接\nSELECT pg_terminate_backend(pid)\nFROM pg_stat_activity\nWHERE datname = 'old_database' AND pid != pg_backend_pid();\n```\n\n## 第四步：自动清理空闲连接\n\n```sql\n-- 配置自动断开 idle in transaction 超时\nALTER SYSTEM SET idle_in_transaction_session_timeout = '10min';\n\n-- 配置 idle 连接超时（PostgreSQL 14+）\nALTER SYSTEM SET idle_session_timeout = '60min';\n\n-- 配置 statement 超时\nALTER SYSTEM SET statement_timeout = '30s';\n\nSELECT pg_reload_conf();\n\n-- 针对特定用户设置\nALTER ROLE app_user SET idle_in_transaction_session_timeout = '5min';\nALTER ROLE app_user SET statement_timeout = '30s';\n```\n\n## 第五步：连接池最佳实践\n\n### PgBouncer 配置\n\n```ini\n[pgbouncer]\npool_mode = transaction\nmax_client_conn = 2000\ndefault_pool_size = 20\nmin_pool_size = 5\nreserve_pool_size = 5\nserver_idle_timeout = 600\nclient_idle_timeout = 300\nserver_connect_timeout = 5\nserver_login_retry = 3\n```\n\n### 连接数规划\n\n```sql\n-- 查看各数据库允许的连接上限\nSELECT datname, datconnlimit,\n       count(pid) AS current_connections\nFROM pg_database\nLEFT JOIN pg_stat_activity USING (datname)\nGROUP BY datname, datconnlimit\nORDER BY current_connections DESC;\n\n-- 限制数据库连接数\nALTER DATABASE mydb CONNECTION LIMIT 100;\n\n-- 限制用户连接数\nALTER ROLE app_user CONNECTION LIMIT 50;\n```",
    },
    {
        "category": "安全与权限",
        "title": "PostgreSQL 安全诊断方案",
        "content": "# PostgreSQL 安全诊断方案\n\n## 概述\n\nPostgreSQL 安全诊断涵盖：认证配置、网络访问控制、权限最小化、审计日志、SQL 注入防护、加密配置等。\n\n## 第一步：认证配置检查\n\n### 调用 `get_os_metrics` skill（需配置 host）\n\n```bash\n# 查看认证配置文件\ncat $PGDATA/pg_hba.conf | grep -v '^#' | grep -v '^$'\n\n# 关注危险配置\ngrep -E 'trust|password' $PGDATA/pg_hba.conf\n```\n\n**安全风险评级**：\n\n| 认证方法 | 风险等级 | 说明 |\n|----------|----------|------|\n| trust | 高危 | 无需密码，仅用于本地开发 |\n| password | 中危 | 明文传输密码 |\n| md5 | 中 | 弱哈希，已被 scram-sha-256 取代 |\n| scram-sha-256 | 低 | 推荐，强密码认证 |\n| peer | 低（本地） | 基于操作系统用户 |\n| cert | 最低 | 基于 SSL 证书，最安全 |\n\n**修复建议**：\n\n```bash\n# pg_hba.conf 推荐配置\n# 本地连接\nlocal   all   postgres   peer\nlocal   all   all        scram-sha-256\n\n# 远程连接（仅允许指定网段，使用 SSL）\nhost    all   all        10.0.0.0/8   scram-sha-256\nhostsl all   all        0.0.0.0/0    scram-sha-256\n\n# 禁止 trust 认证（除了 socket 本地）\n# 删除或注释所有 trust 行\n```\n\n## 第二步：网络访问控制\n\n### 调用 `get_db_variables` skill\n\n```sql\nSELECT name, setting FROM pg_settings\nWHERE name IN (\n  'listen_addresses','port','ssl','ssl_cert_file',\n  'ssl_key_file','ssl_min_protocol_version'\n);\n```\n\n```bash\n# 检查监听地址（不应监听 0.0.0.0 除非有防火墙）\npsql -c \"SHOW listen_addresses;\"\n\n# 确认 SSL 已开启\npsql -c \"SHOW ssl;\"\n\n# 检查防火墙规则\niptables -L -n | grep 5432\nufw status\n```\n\n**SSL 配置**：\n\n```sql\n-- 强制所有远程连接使用 SSL\n-- postgresql.conf\nALTER SYSTEM SET ssl = on;\nALTER SYSTEM SET ssl_min_protocol_version = 'TLSv1.2';\nSELECT pg_reload_conf();\n```\n\n## 第三步：用户权限最小化\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 列出所有用户和角色\nSELECT rolname, rolsuper, rolcreaterole, rolcreatedb,\n       rolcanlogin, rolreplication, rolconnlimit\nFROM pg_roles\nORDER BY rolsuper DESC, rolcanlogin DESC;\n\n-- 查看超级用户（应严格控制）\nSELECT rolname FROM pg_roles WHERE rolsuper = true;\n\n-- 查看各用户的数据库权限\nSELECT datname, has_database_privilege(rolname, datname, 'CONNECT') AS can_connect\nFROM pg_database CROSS JOIN pg_roles\nWHERE rolcanlogin AND datname NOT IN ('template0','template1')\nORDER BY datname, rolname;\n\n-- 查看表级权限\nSELECT grantee, table_schema, table_name, privilege_type\nFROM information_schema.role_table_grants\nWHERE grantee NOT IN ('postgres','PUBLIC')\nORDER BY grantee, table_name;\n```\n\n**最小权限原则**：\n\n```sql\n-- 创建只读用户\nCREATE ROLE readonly_user WITH LOGIN PASSWORD 'strong_password';\nGRANT CONNECT ON DATABASE mydb TO readonly_user;\nGRANT USAGE ON SCHEMA public TO readonly_user;\nGRANT SELECT ON ALL TABLES IN SCHEMA public TO readonly_user;\nALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO readonly_user;\n\n-- 创建应用用户（最小权限）\nCREATE ROLE app_user WITH LOGIN PASSWORD 'strong_password' CONNECTION LIMIT 50;\nGRANT CONNECT ON DATABASE mydb TO app_user;\nGRANT USAGE ON SCHEMA public TO app_user;\nGRANT SELECT, INSERT, UPDATE, DELETE ON specific_table TO app_user;\nGRANT USAGE ON SEQUENCE specific_table_id_seq TO app_user;\n\n-- 撤销 public schema 的默认权限（PostgreSQL 15+ 默认已收紧）\nREVOKE CREATE ON SCHEMA public FROM PUBLIC;\nREVOKE ALL ON DATABASE mydb FROM PUBLIC;\n```\n\n## 第四步：审计日志配置\n\n```sql\n-- 开启详细日志\nALTER SYSTEM SET log_connections = on;\nALTER SYSTEM SET log_disconnections = on;\nALTER SYSTEM SET log_duration = on;\nALTER SYSTEM SET log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h ';\nALTER SYSTEM SET log_statement = 'ddl';  -- 记录所有 DDL\nALTER SYSTEM SET log_min_duration_statement = 1000;  -- 记录慢查询\nSELECT pg_reload_conf();\n\n-- 安装 pgaudit 扩展（企业级审计）\nCREATE EXTENSION pgaudit;\nALTER SYSTEM SET pgaudit.log = 'write,ddl';\nSELECT pg_reload_conf();\n```\n\n## 第五步：密码安全\n\n```sql\n-- 检查空密码用户\nSELECT rolname FROM pg_authid\nWHERE rolcanlogin AND rolpassword IS NULL;\n\n-- 设置密码过期策略\nALTER ROLE app_user VALID UNTIL '2027-01-01';\n\n-- 强制密码复杂度（需要 passwordcheck 扩展）\n-- shared_preload_libraries = 'passwordcheck'\n\n-- 修改默认 postgres 密码\nALTER USER postgres WITH PASSWORD 'new_strong_password';\n```",
    },
    {
        "category": "安全与权限",
        "title": "PostgreSQL 用户权限诊断方案",
        "content": "# PostgreSQL 用户权限诊断方案\n\n## 概述\n\n用户权限管理是 PostgreSQL 安全的核心。本文档提供权限体系的全面诊断方法，包括角色继承、对象权限、行级安全（RLS）等。\n\n## 第一步：角色体系诊断\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 查看所有角色及属性\nSELECT rolname,\n       CASE WHEN rolsuper THEN 'SUPERUSER' ELSE '' END AS superuser,\n       CASE WHEN rolcreaterole THEN 'CREATEROLE' ELSE '' END AS create_role,\n       CASE WHEN rolcreatedb THEN 'CREATEDB' ELSE '' END AS create_db,\n       CASE WHEN rolcanlogin THEN 'LOGIN' ELSE 'NOLOGIN' END AS login,\n       CASE WHEN rolreplication THEN 'REPLICATION' ELSE '' END AS replication,\n       rolconnlimit, rolvaliduntil\nFROM pg_roles\nORDER BY rolsuper DESC, rolcanlogin DESC, rolname;\n\n-- 查看角色成员关系（角色继承树）\nSELECT r.rolname AS role,\n       m.rolname AS member_of,\n       a.admin_option\nFROM pg_auth_members am\nJOIN pg_roles r ON am.member = r.oid\nJOIN pg_roles m ON am.roleid = m.oid\nLEFT JOIN pg_roles a ON am.grantor = a.oid\nORDER BY role, member_of;\n```\n\n## 第二步：数据库和 Schema 权限\n\n```sql\n-- 数据库级权限\nSELECT datname,\n       has_database_privilege('app_user', datname, 'CONNECT') AS connect,\n       has_database_privilege('app_user', datname, 'CREATE') AS create,\n       has_database_privilege('app_user', datname, 'TEMP') AS temp\nFROM pg_database WHERE datname NOT IN ('template0','template1');\n\n-- Schema 权限\nSELECT nspname AS schema,\n       has_schema_privilege('app_user', nspname, 'USAGE') AS usage,\n       has_schema_privilege('app_user', nspname, 'CREATE') AS create\nFROM pg_namespace\nWHERE nspname NOT LIKE 'pg_%' AND nspname != 'information_schema';\n\n-- 表级权限\nSELECT table_schema, table_name, grantee, privilege_type, is_grantable\nFROM information_schema.role_table_grants\nWHERE grantee = 'app_user'\nORDER BY table_schema, table_name;\n\n-- 函数级权限\nSELECT routine_schema, routine_name, grantee, privilege_type\nFROM information_schema.role_routine_grants\nWHERE grantee = 'app_user';\n```\n\n## 第三步：对象权限详细检查\n\n```sql\n-- 使用 \\dp 等价 SQL 查看表权限\nSELECT relname, relacl\nFROM pg_class\nWHERE relkind = 'r'\n  AND relnamespace = 'public'::regnamespace\nORDER BY relname;\n\n-- 解析 ACL 权限字符串\n-- 格式：grantee=privilege/grantor\n-- r=SELECT, w=UPDATE, a=INSERT, d=DELETE, D=TRUNCATE, x=REFERENCES, t=TRIGGER\n\n-- 查看列级权限\nSELECT column_name, privilege_type, grantee\nFROM information_schema.role_column_grants\nWHERE table_name = 'sensitive_table'\nORDER BY column_name, grantee;\n```\n\n## 第四步：行级安全（Row Level Security）\n\n```sql\n-- 查看开启 RLS 的表\nSELECT relname, relrowsecurity, relforcerowsecurity\nFROM pg_class\nWHERE relrowsecurity = true;\n\n-- 查看 RLS 策略\nSELECT schemaname, tablename, policyname,\n       permissive, roles, cmd, qual, with_check\nFROM pg_policies\nORDER BY schemaname, tablename;\n\n-- 创建 RLS 策略示例\nALTER TABLE orders ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY orders_user_policy ON orders\n  USING (user_id = current_user_id())\n  WITH CHECK (user_id = current_user_id());\n\n-- 超级用户绕过 RLS\nALTER TABLE orders FORCE ROW LEVEL SECURITY;  -- 即使表 owner 也受限\n```\n\n## 第五步：权限审计与修复\n\n```sql\n-- 检查 PUBLIC 角色的危险权限\nSELECT 'database' AS object_type, datname AS object_name,\n       has_database_privilege('PUBLIC', datname, 'CREATE') AS public_can_create\nFROM pg_database\nWHERE has_database_privilege('PUBLIC', datname, 'CREATE');\n\n-- 收紧 public schema 权限\nREVOKE CREATE ON SCHEMA public FROM PUBLIC;\nREVOKE ALL ON ALL TABLES IN SCHEMA public FROM PUBLIC;\n\n-- 批量授权模板\n-- 应用用户\nDO $$\nDECLARE\n  t text;\nBEGIN\n  FOR t IN SELECT tablename FROM pg_tables WHERE schemaname = 'public'\n  LOOP\n    EXECUTE format('GRANT SELECT, INSERT, UPDATE, DELETE ON %I TO app_user', t);\n  END LOOP;\nEND;\n$$;\n\n-- 设置 schema 默认权限（新建对象自动继承）\nALTER DEFAULT PRIVILEGES IN SCHEMA public\n  GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO app_user;\nALTER DEFAULT PRIVILEGES IN SCHEMA public\n  GRANT USAGE ON SEQUENCES TO app_user;\nALTER DEFAULT PRIVILEGES IN SCHEMA public\n  GRANT EXECUTE ON FUNCTIONS TO app_user;\n```\n\n## 第六步：权限变更记录\n\n```sql\n-- 开启 DDL 日志记录权限变更\nALTER SYSTEM SET log_statement = 'ddl';\nSELECT pg_reload_conf();\n\n-- 使用 pgaudit 记录权限操作\n-- pgaudit.log = 'role'  记录所有 GRANT/REVOKE/CREATE ROLE 操作\n```",
    },
    {
        "category": "技术参考",
        "title": "PostgreSQL WAL技术细节",
        "content": "# PostgreSQL WAL技术细节\n\n## 概述\n\nWAL（Write-Ahead Logging，预写日志）是 PostgreSQL 数据持久化和复制的核心机制。理解 WAL 的工作原理对于性能调优、故障恢复和复制配置至关重要。\n\n## WAL 基本原理\n\nPostgreSQL 遵循 WAL 协议：**任何数据修改必须先写入 WAL 日志，才能写入数据文件**。这保证了：\n1. **持久性（Durability）**：崩溃后可通过重放 WAL 恢复数据\n2. **一致性（Consistency）**：检查点之间的数据修改通过 WAL 保证\n3. **复制基础**：流复制和逻辑复制均基于 WAL\n\n## WAL 文件结构\n\n```\n$PGDATA/pg_wal/\n├── 000000010000000000000001  # WAL 段文件（默认 16MB）\n├── 000000010000000000000002\n├── archive_status/           # 归档状态目录\n│   ├── 000000010000000000000001.done\n│   └── 000000010000000000000002.ready\n└── ...\n```\n\nWAL 文件命名格式：`TT XXXXXXXX YYYYYYYY`\n- TT：时间线 ID（8位十六进制）\n- XXXXXXXX：高32位 LSN\n- YYYYYYYY：低32位 LSN 的段编号\n\n## LSN（Log Sequence Number）\n\n```sql\n-- 查看当前 WAL 位置\nSELECT pg_current_wal_lsn() AS current_lsn,\n       pg_current_wal_insert_lsn() AS insert_lsn,\n       pg_current_wal_flush_lsn() AS flush_lsn;\n\n-- 计算 WAL 生成量\nSELECT pg_size_pretty(\n  pg_wal_lsn_diff(pg_current_wal_lsn(), '0/1000000')\n) AS wal_generated;\n\n-- 查看 WAL 文件对应的 LSN 范围\nSELECT pg_walfile_name(pg_current_wal_lsn()),\n       pg_walfile_name_offset(pg_current_wal_lsn());\n```\n\n## WAL 级别（wal_level）\n\n### 调用 `get_db_variables` skill\n\n```sql\nSELECT name, setting, short_desc FROM pg_settings\nWHERE name IN ('wal_level','archive_mode','wal_compression',\n               'max_wal_senders','max_replication_slots');\n```\n\n| wal_level | 说明 | 用途 |\n|-----------|------|------|\n| minimal | 最少 WAL，不支持复制 | 仅本地使用（已废弃） |\n| replica | 支持流复制和 pg_basebackup | 默认值，推荐 |\n| logical | 支持逻辑复制 | 需要逻辑解码时使用 |\n\n## 检查点（Checkpoint）\n\n检查点将内存中的 dirty pages 刷到磁盘，并记录检查点位置到 WAL。\n\n```sql\n-- 查看检查点统计\nSELECT checkpoints_timed,  -- 定时触发（正常）\n       checkpoints_req,    -- 手动/WAL满触发（过多说明 max_wal_size 不足）\n       checkpoint_write_time,\n       checkpoint_sync_time,\n       buffers_checkpoint, -- 检查点写出的 buffer 数\n       buffers_backend,    -- 后端直接写的 buffer 数（性能问题信号）\n       buffers_backend_fsync  -- 后端 fsync 次数（IO压力极高信号）\nFROM pg_stat_bgwriter;\n\n-- 手动触发检查点（维护操作前）\nCHECKPOINT;\n```\n\n**调优参数**：\n```sql\n-- 增大 WAL 缓冲区减少检查点频率\nALTER SYSTEM SET max_wal_size = '4GB';\n-- 将检查点 IO 分散到时间窗口\nALTER SYSTEM SET checkpoint_completion_target = 0.9;\nALTER SYSTEM SET checkpoint_timeout = '10min';\nSELECT pg_reload_conf();\n```\n\n## WAL 归档\n\n```sql\n-- 配置归档\nALTER SYSTEM SET archive_mode = on;\nALTER SYSTEM SET archive_command = 'test ! -f /archive/%f && cp %p /archive/%f';\nALTER SYSTEM SET archive_cleanup_command = 'pg_archivecleanup /archive %r';\nSELECT pg_reload_conf();\n\n-- 查看归档状态\nSELECT archived_count, last_archived_wal, last_archived_time,\n       failed_count, last_failed_wal, last_failed_time\nFROM pg_stat_archiver;\n\n-- 手动归档（通知 PostgreSQL 切换 WAL 段）\nSELECT pg_switch_wal();\n```\n\n## 流复制 WAL 发送\n\n```sql\n-- 查看 WAL sender 状态\nSELECT pid, usename, application_name, client_addr,\n       state, sent_lsn, write_lsn, flush_lsn, replay_lsn,\n       write_lag, flush_lag, replay_lag,\n       sync_state\nFROM pg_stat_replication;\n\n-- 查看复制槽（防止 WAL 被提前删除）\nSELECT slot_name, slot_type, active,\n       pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) AS wal_retained\nFROM pg_replication_slots;\n```\n\n## pg_waldump 工具\n\n```bash\n# 解析 WAL 内容（用于调试和审计）\npg_waldump -p $PGDATA/pg_wal \\\n  -s 0/1234567 \\\n  -e 0/1234890 \\\n  -n 100\n\n# 查看特定关系的 WAL\npg_waldump -p $PGDATA/pg_wal \\\n  --relation=1663/16384/16385 \\\n  000000010000000000000001\n```\n\n## WAL 性能监控\n\n```sql\n-- WAL 写入性能\nSELECT name, setting FROM pg_settings\nWHERE name IN ('fsync','wal_sync_method','synchronous_commit',\n               'commit_delay','commit_siblings','wal_writer_delay');\n\n-- wal_sync_method 选项：\n-- open_datasync: write/sync（Linux 默认）\n-- fdatasync: fdatasync\n-- fsync_writethrough: 写透（macOS）\n-- open_sync: O_SYNC\n```",
    },
    {
        "category": "技术参考",
        "title": "PostgreSQL 错误码查询",
        "content": "# PostgreSQL 错误码查询\n\n## 概述\n\nPostgreSQL 使用 5 位 SQLSTATE 错误码（兼容 SQL 标准）。前2位为错误类别，后3位为具体错误。本文档列出 DBA 最常遇到的错误码及处理方法。\n\n## 错误码查询方法\n\n### 调用 `execute_diagnostic_query` skill\n\n```sql\n-- 在应用层捕获 SQLSTATE\n-- Python psycopg2: except psycopg2.Error as e: print(e.pgcode)\n-- Java JDBC: catch(SQLException e) { e.getSQLState(); }\n\n-- 在 PL/pgSQL 中捕获和处理错误\nDO $$\nBEGIN\n  BEGIN\n    INSERT INTO test_unique(id) VALUES (1);\n    INSERT INTO test_unique(id) VALUES (1);\n  EXCEPTION\n    WHEN unique_violation THEN\n      RAISE NOTICE 'Caught 23505: unique_violation';\n    WHEN foreign_key_violation THEN\n      RAISE NOTICE 'Caught 23503: foreign_key_violation';\n  END;\nEND;\n$$;\n\n-- 查看错误日志配置\nSELECT name, setting FROM pg_settings\nWHERE name IN ('log_min_error_statement','log_error_verbosity','log_min_duration_statement');\n```\n\n## 类别 08：连接异常\n\n| 错误码 | 名称 | 说明 | 处理方向 |\n|--------|------|------|----------|\n| 08000 | connection_exception | 连接异常 | 检查网络和 pg_hba.conf |\n| 08003 | connection_does_not_exist | 连接不存在 | 重新连接 |\n| 08006 | connection_failure | 连接失败 | 检查服务器状态 |\n| 08004 | sqlserver_rejected_establishment | 服务器拒绝连接 | 检查 max_connections 和 pg_hba.conf |\n\n## 类别 23：完整性约束违反\n\n| 错误码 | 名称 | 说明 | 处理方向 |\n|--------|------|------|----------|\n| 23502 | not_null_violation | NOT NULL 约束违反 | 检查传参是否有 null |\n| 23503 | foreign_key_violation | 外键约束违反 | 确保父记录存在后再插入子记录 |\n| 23505 | unique_violation | 唯一约束违反 | 使用 INSERT ... ON CONFLICT |\n| 23514 | check_violation | CHECK 约束违反 | 检查约束定义和数据范围 |\n| 23P01 | exclusion_violation | 排除约束违反 | 检查范围/几何类型冲突 |\n\n```sql\n-- 处理唯一冲突\nINSERT INTO user(email, name) VALUES ('test@example.com', 'Test')\nON CONFLICT (email) DO UPDATE SET name = EXCLUDED.name;\n\n-- 查看约束定义\nSELECT conname, contype, pg_get_constraintdef(oid)\nFROM pg_constraint WHERE conrelid = 'your_table'::regclass;\n```\n\n## 类别 40：事务回滚\n\n| 错误码 | 名称 | 说明 | 处理方向 |\n|--------|------|------|----------|\n| 40001 | serialization_failure | 序列化失败（并发冲突） | 应用层重试（指数退避） |\n| 40P01 | deadlock_detected | 死锁 | 优化加锁顺序，参考死锁诊断文档 |\n\n## 类别 42：语法或访问规则\n\n| 错误码 | 名称 | 说明 | 处理方向 |\n|--------|------|------|----------|\n| 42601 | syntax_error | SQL 语法错误 | 检查 SQL 语句，使用 EXPLAIN 验证 |\n| 42501 | insufficient_privilege | 权限不足 | GRANT 相应权限给用户 |\n| 42703 | undefined_column | 列不存在 | 检查列名拼写和别名 |\n| 42P01 | undefined_table | 表不存在 | 检查表名和 search_path |\n| 42883 | undefined_function | 函数不存在 | 检查函数名和参数类型 |\n| 42P07 | duplicate_table | 表已存在 | 使用 CREATE TABLE IF NOT EXISTS |\n| 42701 | duplicate_column | 列已存在 | 使用 ADD COLUMN IF NOT EXISTS |\n\n```sql\n-- 查看 search_path 影响\nSHOW search_path;\nSET search_path = public, myschema;\n\n-- 列出所有 public schema 下的表\nSELECT tablename FROM pg_tables WHERE schemaname = 'public';\n```\n\n## 类别 53：资源不足\n\n| 错误码 | 名称 | 说明 | 处理方向 |\n|--------|------|------|----------|\n| 53100 | disk_full | 磁盘满 | 立即清理磁盘：VACUUM，删旧日志 |\n| 53200 | out_of_memory | 内存不足 | 降低 work_mem，增加物理内存 |\n| 53300 | too_many_connections | 连接数超限 | 增大 max_connections 或部署 PgBouncer |\n\n## 类别 57：操作干预\n\n| 错误码 | 名称 | 说明 | 处理方向 |\n|--------|------|------|----------|\n| 57014 | query_canceled | 查询被取消 | 检查 statement_timeout 设置 |\n| 57P01 | admin_shutdown | 管理员关闭服务 | 等待服务重启 |\n| 57P03 | cannot_connect_now | 服务启动中无法连接 | 等待启动完成 |\n\n## 类别 55：对象状态错误\n\n| 错误码 | 名称 | 说明 | 处理方向 |\n|--------|------|------|----------|\n| 55006 | object_in_use | 对象正在被使用 | 等待其他会话释放 |\n| 55P03 | lock_not_available | 无法获取锁 | 检查 lock_timeout，分析锁等待 |\n\n## 类别 58：系统错误\n\n| 错误码 | 名称 | 说明 | 处理方向 |\n|--------|------|------|----------|\n| 58000 | system_error | 系统错误 | 查看系统日志 |\n| 58030 | io_error | IO 错误 | 检查磁盘健康状态 |\n\n## 快速诊断查询\n\n```sql\n-- 查看数据库级错误统计\nSELECT datname, xact_rollback, deadlocks, conflicts\nFROM pg_stat_database \n-- 查看当前等待的锁（可能导致错误）\nSELECT pid, wait_event_type, wait_event, left(query, 100)\nFROM pg_stat_activity WHERE wait_event_type IS NOT NULL;\n\n-- 配置详细错误日志\nALTER SYSTEM SET log_min_error_statement = 'error';\nALTER SYSTEM SET log_error_verbosity = 'verbose';\nSELECT pg_reload_conf();\n```",
    },
]
