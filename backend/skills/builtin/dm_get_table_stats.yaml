id: dm_get_table_stats
version: 1.0.0
category: dm
name: dm_get_table_stats
description: Get DM table statistics from DBA_TABLES
tags:
  - dm
  - tables
  - statistics
parameters:
  - name: datasource_id
    type: integer
    required: true
    description: ID of the DM datasource
timeout: 30
permissions:
  - read_datasource
code: |
  async def execute(context, params):
      datasource_id = params['datasource_id']

      from backend.models.datasource import Datasource
      from backend.services.dm_service import DMConnector
      from backend.utils.encryption import decrypt_value

      # Get datasource
      datasource = await context.get_connection(datasource_id)
      if not datasource:
          return {"success": False, "error": "Datasource not found"}

      if datasource.db_type != "dm":
          return {"success": False, "error": "Not a DM datasource"}

      # Decrypt password
      password = decrypt_value(datasource.password_encrypted) if datasource.password_encrypted else None

      # Connect and get table stats
      connector = DMConnector(
          host=datasource.host,
          port=datasource.port,
          username=datasource.username,
          password=password,
          database=datasource.database
      )

      table_stats = await connector.get_table_stats()
      return {"success": True, "data": table_stats}
