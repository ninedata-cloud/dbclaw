id: mysql_get_db_size
name: MySQL Database Size
version: 1.0.0
author: DBClaw
category: mysql
description: Get MySQL database size information including data and index sizes
tags: [mysql, storage, size, capacity]

parameters:
  - name: datasource_id
    type: integer
    required: true
    description: The database connection ID

permissions:
  - execute_query

code: |
  async def execute(context, params):
      datasource_id = params['datasource_id']

      # Get database sizes
      query = """
      SELECT
          table_schema AS 'database',
          ROUND(SUM(data_length) / 1024 / 1024, 2) AS 'data_size_mb',
          ROUND(SUM(index_length) / 1024 / 1024, 2) AS 'index_size_mb',
          ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) AS 'total_size_mb'
      FROM information_schema.tables
      WHERE table_schema NOT IN ('information_schema', 'mysql', 'performance_schema', 'sys')
      GROUP BY table_schema
      ORDER BY total_size_mb DESC
      """

      result = await context.execute_query(query, datasource_id)

      if not result.get("success"):
          return {"success": False, "error": result.get("error", "Failed to get database sizes")}

      return {
          "success": True,
          "databases": result.get("data", []),
          "columns": result.get("columns", [])
      }

i18n:
  zh-CN:
    name: MySQL 数据库容量
    description: 执行“MySQL 数据库容量”操作，返回用于诊断和运维分析的结构化结果。
    parameter_descriptions:
      datasource_id: 数据源 ID。
    permission_descriptions:
      execute_query: 允许执行受控数据库查询
  en-US:
    name: MySQL Database Size
    description: Get MySQL database size information including data and index sizes
    parameter_descriptions:
      datasource_id: The database connection ID
    permission_descriptions:
      execute_query: Execute governed database queries
