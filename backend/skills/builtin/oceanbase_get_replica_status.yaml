id: oceanbase_get_replica_status
version: 1.0.0
category: oceanbase
name: oceanbase_get_replica_status
description: Get OceanBase replica lag and health status
tags:
  - oceanbase
  - replication
  - monitoring
parameters:
  - name: datasource_id
    type: integer
    required: true
    description: ID of the OceanBase datasource
timeout: 30
permissions:
  - read_datasource
code: |
  async def execute(context, params):
      datasource_id = params['datasource_id']

      from backend.models.datasource import Datasource
      from backend.services.oceanbase_service import OceanBaseConnector
      from backend.utils.encryption import decrypt_value

      # Get datasource
      datasource = await context.db.get(Datasource, datasource_id)
      if not datasource:
          return {"success": False, "error": "Datasource not found"}

      if datasource.db_type != "oceanbase":
          return {"success": False, "error": "Not an OceanBase datasource"}

      # Decrypt password
      password = decrypt_value(datasource.password_encrypted) if datasource.password_encrypted else None

      # Connect and get replica status
      connector = OceanBaseConnector(
          host=datasource.host,
          port=datasource.port,
          username=datasource.username,
          password=password,
          database=datasource.database
      )

      replica_status = await connector.get_replication_status()
      return {"success": True, "data": replica_status}
