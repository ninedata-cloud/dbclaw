id: opengauss_list_connections
version: 1.0.0
category: opengauss
name: opengauss_list_connections
description: Get active connections from openGauss pg_stat_activity
tags:
  - opengauss
  - connections
  - monitoring
parameters:
  - name: datasource_id
    type: integer
    required: true
    description: ID of the openGauss datasource
timeout: 30
permissions:
  - read_datasource
code: |
  async def execute(context, params):
      datasource_id = params['datasource_id']

      from backend.models.datasource import Datasource
      from backend.services.opengauss_service import OpenGaussConnector
      from backend.utils.encryption import decrypt_value

      # Get datasource
      datasource = await context.get_connection(datasource_id)
      if not datasource:
          return {"success": False, "error": "Datasource not found"}

      if datasource.db_type != "opengauss":
          return {"success": False, "error": "Not an openGauss datasource"}

      # Decrypt password
      password = decrypt_value(datasource.password_encrypted) if datasource.password_encrypted else None

      # Connect and get process list
      connector = OpenGaussConnector(
          host=datasource.host,
          port=datasource.port,
          username=datasource.username,
          password=password,
          database=datasource.database
      )

      connections = await connector.get_process_list()
      return {"success": True, "data": connections}
