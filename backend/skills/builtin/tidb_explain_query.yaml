id: tidb_explain_query
version: 1.0.0
category: tidb
name: tidb_explain_query
description: Get query execution plan for TiDB using EXPLAIN ANALYZE
tags:
  - tidb
  - query
  - performance
parameters:
  - name: datasource_id
    type: integer
    required: true
    description: ID of the TiDB datasource
  - name: query
    type: string
    required: true
    description: SQL query to explain
timeout: 30
permissions:
  - read_datasource
code: |
  async def execute(context, params):
      datasource_id = params['datasource_id']

      from backend.models.datasource import Datasource
      from backend.services.tidb_service import TiDBConnector
      from backend.utils.encryption import decrypt_value

      # Get datasource
      datasource = await context.db.get(Datasource, datasource_id)
      if not datasource:
          return {"success": False, "error": "Datasource not found"}

      if datasource.db_type != "tidb":
          return {"success": False, "error": "Not a TiDB datasource"}

      # Decrypt password
      password = decrypt_value(datasource.password_encrypted) if datasource.password_encrypted else None

      # Connect and explain query
      connector = TiDBConnector(
          host=datasource.host,
          port=datasource.port,
          username=datasource.username,
          password=password,
          database=datasource.database
      )

      plan = await connector.explain_query(query)
      return {"success": True, "data": plan}
