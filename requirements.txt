# Web framework
fastapi>=0.115.0
uvicorn>=0.30.0

# Database (async SQLAlchemy + PostgreSQL)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0          # PostgreSQL / openGauss (primary metadata store)
psycopg2-binary>=2.9.0   # PostgreSQL sync driver (for data migration script)

# Database drivers (optional, install as needed)
aiomysql>=0.2.0          # MySQL / MariaDB
motor>=3.3.0              # MongoDB
redis>=5.0.0              # Redis
pyodbc>=5.0.0             # SQL Server
pymssql>=2.2.0            # SQL Server (alternative)
oracledb>=1.0.0           # Oracle
# dmPython                # DM (Dameng) - install manually

# AI / LLM
openai>=1.0.0

# Vector store & embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Authentication & security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
cryptography>=42.0.0

# Settings & config
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Scheduling
apscheduler>=3.10.0

# HTTP client
aiohttp>=3.9.0
requests>=2.31.0

# SSH
paramiko>=3.0.0

# Document parsing & export
pyyaml>=6.0
pillow>=10.0.0
reportlab>=4.0.0
markdown-it-py>=3.0.0
python-docx>=1.1.0
python-multipart>=0.0.9
aiofiles>=23.0.0
